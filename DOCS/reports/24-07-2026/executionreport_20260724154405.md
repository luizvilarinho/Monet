# Execution Report

## Documents
- Task report: DOCS/reports/24-07-2026/taskreport_20260724153714.md
- Task report (extensão acadêmica): DOCS/reports/24-07-2026/taskreport_20260724172148.md
- Execution report: DOCS/reports/24-07-2026/executionreport_20260724154405.md
- Explorer report (pipeline web): DOCS/reports/24-07-2026/explorerreport_20260724154755.md
- Explorer report (extensão acadêmica): DOCS/reports/24-07-2026/explorerreport_20260724172653.md
- Plan report (pipeline web): DOCS/reports/24-07-2026/planreport_20260724160318.md
- Plan report (extensão acadêmica): DOCS/reports/24-07-2026/planreport_20260724174842.md
- Review report: DOCS/reports/24-07-2026/reviewreport_20260724190643.md

## Sessions
### Orchestrator — abertura — 20260724154405
- Summary: Task Report aprovado pelo Coordenador. Task é a reimplementação da persistência de fontes web via arquitetura de "bibliotecário" (pipeline em background orquestrado por código, DeepSeek para triagem/julgamento com saída JSON), substituindo a coreografia de tool-calling multi-rodada (fonte de 5 sessões de bug documentadas em DOCS/reports/23-07-2026/). Decisões de escopo travadas pelo Coordenador antes do Task Report: (1) modo explícito + oportunista já no v1; (2) conteúdo salvo é o markdown raw do Tavily verbatim, sem reescrita por modelo (modelo só julga); (3) modelo secundário = DeepSeek via OpenRouter. Infra da implementação anterior (comandos Tauri, tavilyExtract/hashUrlToFilename, RAG dinâmico pasta→filhos, badge AI-generated, deleção física condicional) é preservada e reaproveitada; só a coreografia dentro de useChat.ts é removida. Sem passos de git (feito na própria branch feature/websearch-rag, por decisão do Coordenador).
- Decision: pipeline explorer → planner → code (recomendação do Orquestrador, aprovada junto do Task Report). ExplorerAgent primeiro porque a infra a preservar e a coreografia a remover estão entrelaçadas no mesmo arquivo (useChat.ts) e os pontos exatos de corte + os pontos de disparo do pipeline em background precisam ser mapeados com precisão antes do plano.
- Next step: spawnar explorerAgent com Task Report + path deste Execution Report + docs.menu.md.

### ExplorerAgent — 20260724154755
- Summary: Viabilidade Média. Remoção da coreografia multi-rodada é limpa — web_search/deep_research/update_folder_memory nunca encadearam; o loop existe só para read/save_web_source, e há versão pré-feature single-round no branch main para reverter. Mapeados os pontos de corte exatos (linhas) e o que preservar (webResearchEnabled + expansão dinâmica pasta→filhos no RAG). Três decisões arquiteturais bloqueiam o plano: (1) "disparar logo após web_search" x "prioridade por URL citada" são incompatíveis no tempo — recomendo disparar no fim do turno (onOpenRouterDone); (2) o chat tem DOIS caminhos de busca (modelos com tools no handler / sem tools no bloco pré-turno) e o pipeline precisa captar dos dois; (3) sem a tool save_web_source, o código perdeu o sinal de modo "explícito" — falta definir como distinguir explícito x oportunista. Precedente forte para o modelo secundário JSON: deep_research_generate_sub_queries (lib.rs:401) já faz chamada non-stream ao OpenRouter com response_format json_schema strict + parsing tolerante — reaproveitável para DeepSeek. Toast "N fontes salvas" = padrão folderMemoryUpdatedAt pronto, sem infra nova. Desalinhamentos com infra "a preservar": documents_write_ai_source_file NÃO é atômico hoje (fs::write direto, documents.rs:1129) — precisa temp→rename para cumprir o Constraint; e onOpenRouterDone não enxerga containingFolder/search results (handlers registrados fora do closure de send()), exigindo um librarianContextRef por turno. Estado in-flight deve viver num módulo singleton, não em ref de hook (múltiplas instâncias de useChat por painel).
- Output: DOCS/reports/24-07-2026/explorerreport_20260724154755.md

### Orchestrator — decisão pós-explorer — 20260724155830
- Summary: Explorer entregou mapa preciso (viabilidade Média, remoção da coreografia limpa). Orquestrador resolveu os itens técnicos e o Coordenador decidiu o único ponto de produto (detecção de intent).
- Inputs:
  - DOCS/reports/24-07-2026/explorerreport_20260724154755.md
- Decision (Orquestrador, dentro do escopo aprovado):
  1. Disparar o pipeline no fim do turno (`onOpenRouterDone`), não no retorno do `web_search` — satisfaz zero-latência (Constraint já dizia "após o fim do turno") e habilita prioridade-por-citação. Exige `librarianContextRef` por turno, populado nos DOIS caminhos de busca (modelos com tools no handler / sem tools no bloco pré-turno) e consumido/limpo em done/error/cancel.
  2. Escrita atômica (temp→rename) em `documents_write_ai_source_file` — cumpre o Constraint de atomicidade ("preservar" = não reconstruir, não impede adicionar atomicidade).
  3. Estado in-flight (Set de hashes de URL + serialização por pasta) num módulo singleton `src/lib/librarian.ts`, não em ref de hook (cada painel tem instância própria de `useChat`).
  4. 2 comandos Rust novos espelhando `deep_research_generate_sub_queries` (triagem + julgamento) com modelo DeepSeek + `json_schema`; planner confirma se DeepSeek honra `json_schema` strict ou precisa `json_object` + parsing tolerante (nunca gravar sem decisão parseada).
  5. Toast "N fontes salvas" copia o padrão `folderMemoryUpdatedAt` (sem infra nova). Acoplamento módulo↔hook via callback passado ao pipeline.
  6. Manter o fix de `isStreaming`/`activeStreamRef` (não reintroduzir a linha "clear streaming early" pré-feature, causa do bug #3) ao reverter o handler para single-round.
- Decision (Coordenador — redução de escopo do Task Report): **só modo oportunista no v1.** Sem detecção de intent, sem modo explícito, todas as falhas silenciosas, uma barra de relevância conservadora na triagem. Feedback exclusivamente via toast "N fontes salvas" (nada em caso de falha). Isso elimina o item §6.1 do explorer e a necessidade de distinguir explícito x oportunista. Task Report NÃO reeditado (regra do papel); a redução fica registrada aqui e é passada explicitamente ao planner.
- Next step: spawnar plannerAgent com Task Report + este Execution Report + explorer report + esta decisão de escopo.

### PlannerAgent — 20260724160318
- Summary: Plano gerado em 12 etapas cobrindo as 5 fases (A: limpeza da coreografia em useChat.ts revertendo ao handler single-round de main + mantendo o fix do bug #3; B: comandos Rust librarian_triage/librarian_judge com DeepSeek + escrita atômica temp→rename; C: módulo singleton src/lib/librarian.ts com Set in-flight + serialização por pasta e pipeline dedupe→triagem→extract→julgamento→escrita verbatim→scan→callback; D: librarianContextRef nos dois caminhos de busca + disparo em onOpenRouterDone + estado webSourcesSaved; E: toast "N sources saved" em ChatPanel). Todas as assinaturas reais confirmadas por leitura de código (linhas citadas no plano). Confirmações-chave: usar response_format json_object (NÃO json_schema strict) para DeepSeek + parsing tolerante; scan ignora arquivos .tmp (detect_mime None) → atomicidade segura; fs::rename no Windows substitui destino (MOVEFILE_REPLACE_EXISTING); criação preguiçosa da AI folder (só com ≥1 aprovada) para evitar pastas vazias no modo oportunista.
- Output: DOCS/reports/24-07-2026/planreport_20260724160318.md

### Orchestrator — aprovação do plano + execução — 20260724161654
- Summary: Coordenador aprovou o plano de 12 etapas. Orquestrador validou o plano (fundamentado em código real, respeita as decisões travadas, notas anti-overengineering presentes).
- Inputs:
  - DOCS/reports/24-07-2026/planreport_20260724160318.md
- Decision (Coordenador confirmou os 2 defaults apresentados):
  1. Modelo DeepSeek = `deepseek/deepseek-chat` (id fixo/interno em lib.rs; trocável por 1 constante).
  2. Deep research FORA do escopo do bibliotecário no v1 — só `web_search`/`webSearch` alimentam o pipeline.
  - Confirmação técnica load-bearing do plano: `response_format: json_object` (não `json_schema` strict) para DeepSeek + parsing tolerante.
- Next step: Passo 4 Etapa 1 — spawnar codeAgent com Task Report + este Execution Report + explorer report + plan report. Após conclusão, sinalizar ao Coordenador para teste manual antes da Etapa 2 (review).

### CodeAgent — implementação inicial — 20260724163206
- Summary: Implementadas as 12 etapas do plano (substituição da coreografia multi-rodada por pipeline "bibliotecário" em background). (1) `useChat.ts` revertido ao handler single-round de `main` (rodada 1 com tools → rodada 2 sem tools), MANTENDO o fix do bug #3 (não reintroduzidas as 3 linhas "clear streaming early" — handler começa direto no `try`). Splice feito via script Python por line-range (2228–2612) para evitar erro de exact-match em bloco de ~385 linhas; demais alterações via edições anchor-based. (2) Removidos `MAX_TOOL_ROUNDS`, `completedActionsRef` (+ cascata em `onOpenRouterDone`), estados `savingWebSource`/`readingWebSource` (interface, useState, resets em done/error/failOnAssistant/cancel, retorno) e todos os `console.log` `ROUND_TRACE:`/`SAVE_WEB_SOURCE:`/`SAVE_WEB_SEARCH`. (3) Indicadores de UI `readingWebSource`/`savingWebSource` removidos de `ChatPanel.tsx`. (4) Escrita atômica temp→rename em `documents_write_ai_source_file` (sufixo `.md.tmp` que o scan ignora; remove `.tmp` órfão em falha de rename). (5) `librarian_triage`/`librarian_judge` em `lib.rs` (helper `librarian_decide` compartilhado, `LIBRARIAN_MODEL="deepseek/deepseek-chat"`, `response_format: json_object`, parsing tolerante, `Ok(vec![])` em qualquer falha) + registro no `generate_handler!`. (6+7) Novo módulo singleton `src/lib/librarian.ts`: wrappers `invoke`, `inFlightUrls` Set + `folderChains` Map, `enqueueLibrarian` (serialização por pasta, fire-and-forget) e `runPipeline` (dedupe disco+in-flight → triagem → extract em lote → julgamento → escrita VERBATIM do rawContent + cabeçalho de proveniência → scan fire-and-forget → toast; try/finally libera in-flight; criação preguiçosa da AI folder). (8) `librarianContextRef` por turno populado nos DOIS caminhos de busca (branch `web_search` do handler + bloco pré-turno via `preTurnSearchResults` após resolução de `containingFolder`); limpo em send()/error/cancel. (9) Disparo em `onOpenRouterDone` com prioridade-por-citação best-effort (lê texto final de `conversationsRef`) + callbacks `onSaved`/`onFolderRegistered` (registra AI folder na seleção via `setFolders`). (10) Estado `webSourcesSaved` espelhando `folderMemoryUpdatedAt`. (11) Toast "N source(s) saved to the knowledge base" em `ChatPanel.tsx` (inglês, reusa `styles.webSearchProgress`/`webSearchDot`).
- Desvios do plano: nenhum de escopo. Ajuste inequívoco documentado: um comentário stale do bloco RAG citava `save_web_source` ("criada automaticamente por save_web_source") — atualizado para "pelo pipeline do bibliotecário" para zerar o grep de resíduos (o código do RAG permaneceu intocado). Deep research permanece FORA do pipeline (só webSearch/web_search alimentam o ref). Nada de git (branch atual).
- Validação: `npx tsc --noEmit` limpo (exit 0). `npx vite build` concluído (exit 0; avisos pré-existentes de chunk size e de import misto de `documents.ts` — agora incluindo `librarian.ts` — aceitáveis). `cargo check` limpo (exit 0). Grep de resíduos (13 termos) = 0 ocorrências em `src/` e `src-tauri/`. Preservados e confirmados: `webResearchEnabled` (13 refs em useChat), expansão RAG pasta→filhos + gate `origin==='ai'`, tools `web_search`/`deep_research`/`update_folder_memory`, `setFolderVisibleDocuments` (ainda consumido por Assistant/Chat/Reader panels — não orfanado).
- Files changed:
  - src/hooks/useChat.ts
  - src/components/ChatPanel/ChatPanel.tsx
  - src-tauri/src/documents.rs
  - src-tauri/src/lib.rs
- Files created:
  - src/lib/librarian.ts
- Files removed:
  - nenhum
- Left behind: nenhum. Pendências conhecidas registradas como riscos do plano (não são bugs): crescimento monotônico da KB sem poda/eviction (barra de triagem conservadora é o único freio); prioridade-por-citação best-effort (raça last-chunk/done só afeta ordenação, não correção); janela save↔RAG (fonte pode não estar indexada no turno imediatamente seguinte). Recomenda-se teste manual do fluxo real (web search com pasta `webResearchEnabled` ligado) antes da etapa de review.

### Orchestrator — expansão de escopo (extensão acadêmica) — 20260724172148
- Summary: Coordenador testou a implementação inicial (pipeline web) e confirmou que funciona. Antes do review (Etapa 2), propôs uma extensão: o bibliotecário também buscar artigos científicos em acesso aberto (Crossref → Unpaywall/DOAJ), baixar o PDF real e gravar na mesma pasta de KB da IA (a ingestão de PDF já existe via pdf_extract). Orquestrador avaliou a viabilidade a pedido do Coordenador: estrategicamente melhor encaixe com productVision (material de estudo real, não internet genérica) e legalmente limpo (só OA); tecnicamente viável mas é um sub-pipeline NOVO com superfície de backend nova (download de binário + validação %PDF no Rust por causa de CORS; comando de escrita binária/.pdf; extração de termos + filtro de relevância contra ruído do Crossref). Riscos sinalizados: taxa de OA ~50%, abstract ausente no Crossref, PDF escaneado sem texto, imposição/poluição (cap conservador necessário).
- Inputs:
  - taskreport_20260724172148.md (novo Task Report da extensão)
- Decision (Coordenador — duas escolhas de produto):
  1. Acadêmico **complementa** o web (ambos os pipelines ativos, gravando na mesma pasta).
  2. Gatilho **proativo** em todo turno de pesquisa (toda web search com a flag ligada também tenta Crossref).
- Decision (Orquestrador — processo): tratar como novo ciclo do pipeline (Task Report novo → explorer → planner → code) MAS dentro deste mesmo Execution Report, porque é a mesma feature/branch do bibliotecário e o review da Etapa 2 cobrirá os dois pipelines de uma vez. Review do pipeline web NÃO roda isolado agora; será unificado após a extensão acadêmica entrar.
- Next step: aguardando aprovação do novo Task Report pelo Coordenador. Se aprovado, spawnar explorerAgent (foco: cliente HTTP para download binário no Rust, shape do comando de escrita binária/.pdf, especificidades Crossref/Unpaywall/DOAJ, ponto de extração de termos + filtro de relevância, cap por turno).

### ExplorerAgent — 20260724172653
- Summary: Viabilidade Média, sem bloqueadores — trabalho aditivo, nada refatora no pipeline web. (1) reqwest 0.12 já basta para o download binário: `.bytes()` (core), redirect (default até 10), `.user_agent()`/`.timeout()` via Client::builder — NADA a adicionar no Cargo.toml; única novidade vs. o código atual é ler `.bytes()` em vez de `.json()/.text()`. (2) Recomendo UM comando Rust único `librarian_fetch_and_save_pdf(folder_id,url,filename)` (baixa→valida `%PDF`→escreve atômico) para os bytes nunca cruzarem o IPC, reusando o temp→rename via helper `write_bytes_atomic` compartilhado com o comando `.md` (documents.rs:1129) — manter o `documents_write_ai_source_file` de `.md` intacto. (3) Crossref+Unpaywall retornam JSON com CORS aberto → hospedar no frontend (fork para Rust se teste de CORS no webview tauri://localhost falhar); DOAJ FORA do v1 (link é HTML, morre no `%PDF`). Download SEMPRE no Rust. (3b) `%PDF` é `bytes.starts_with(b"%PDF")`, trivial; PDF escaneado passa no magic mas o guard documents.rs:520 já retorna erro e NÃO indexa (vira arquivo morto cosmético) — Opção 1 (aceitar) recomendada vs. Opção 2 (detectar antes, exige extrair do .tmp). (4) As duas etapas de modelo (gerar query Crossref + julgar relevância título+abstract) reaproveitam `librarian_decide` (lib.rs:528) com prompts novos — 2 comandos finos ao lado de librarian_triage/judge, cap 3 reforçado no TS. (5) Encaixe: ESTENDER runPipeline (não novo enqueue) rodando a fase acadêmica sequencial após o web, mesma AI folder, `inFlightUrls` com chave `doi:`, savedCount SOMADO, um scan e um onSaved só; o `question` do librarianContextRef já basta, nada a mudar no useChat/enqueueLibrarian. (6) Sem colisão de nomes: web=`<sha256>.md`, acadêmico=`{ano}_{slug}.pdf`; dedupe reusa existingFilenames (disco) + inFlightUrls (in-flight), slug sanitizado no TS. Riscos novos não no Task Report: CORS/origin do webview (testável em 1 min), abstract vem em JATS-XML (precisa strip de tags), pdf_extract só aceita path (impacta Opção 2 do §3b), latência serial do sub-pipeline. Decisões que exigem o Coordenador: (a) PDF escaneado aceitar-como-morto vs. detectar-antes; (b) abstract ausente julgar-por-título-conservador vs. buscar-externo (fora do v1). Ambas de baixo risco, apresentáveis como defaults.
- Output: DOCS/reports/24-07-2026/explorerreport_20260724172653.md

### Orchestrator — aprovação da extensão + exploração — 20260724172459
- Summary: Coordenador aprovou o Task Report da extensão acadêmica e definiu os 2 pontos abertos: e-mail de contato Crossref/Unpaywall = luizvilarinho@gmail.com (fixo); cap = 3 papers por turno. Task Report NÃO reeditado (já compatível com essas escolhas); ambas passadas explicitamente ao explorer.
- Next step: explorerAgent da extensão disparado (foco: cliente HTTP p/ download binário no Rust, comando de escrita binária/.pdf, APIs Crossref/Unpaywall/DOAJ + %PDF + PDF escaneado, extração de termos + filtro de relevância via padrão librarian_decide, encaixe no runPipeline). Aguardando relatório.

### PlannerAgent — 20260724174842
- Summary: Plano da extensão acadêmica gerado em 9 etapas, respeitando todas as decisões travadas (só Crossref+Unpaywall; DOAJ e detecção de PDF escaneado fora; e-mail fixo; cap 3 reforçado no TS; PDF verbatim; sem mexer no Cargo.toml/useChat.ts/enqueueLibrarian/comando .md web). Fase A backend Rust: (1) extrair helper `write_bytes_atomic(dir, safe_name, &bytes)` do bloco atômico do comando .md e refatorar `documents_write_ai_source_file` para usá-lo (comportamento .md inalterado); (2) comando único `librarian_fetch_and_save_pdf(folder_id, url, filename)` — Client::builder com user_agent+timeout(30s)+redirect default, `.bytes()`, checa MAX_FILE_BYTES e `starts_with(b"%PDF")`, grava via helper, bytes nunca cruzam IPC; (3) `librarian_academic_query` (field "queries") + `librarian_academic_relevance` (field "dois", barra extra-conservadora p/ abstract ausente, cap 3) via `librarian_decide`; (4) registro no generate_handler!. Fase B frontend librarian.ts: (5) wrappers TS dos 3 comandos; (6) crossrefSearch/unpaywallResolve via fetch + strip JATS (regex `<[^>]+>`) + polite pool via mailto query param (User-Agent é forbidden header no webview — não setar), COM teste de CORS na sub-etapa 6.0 decidindo o fork p/ comandos Rust irmãos de web_search_route se falhar em tauri://localhost; (7) academicSlug/academicFilename `{ano}_{slug}.pdf`; (8) reestruturar runPipeline extraindo runWebPhase/runAcademicPhase (early-returns não abortam mais o pipeline inteiro — problema real detectado: os `return` do bloco web pulariam a fase acadêmica), ensureFolder memoizado (criação preguiçosa), inFlightUrls com chave `doi:` + filename (TOCTOU), throttle 250ms no loop Unpaywall, savedCount SOMADO, UM scan e UM onSaved no fim. Fase C: (9) tsc/vite/cargo + teste manual. Todas as assinaturas reconfirmadas por leitura (documents.rs:1090/1134-1140, lib.rs:528/605/1595, detect_mime:219, librarian.ts:59-156, search.ts:132, documents.ts:103-117). Nenhuma mudança de escopo. Pronto para execução — as 2 escolhas de produto já travadas; único fork é técnico e auto-decidido pelo teste de CORS.
- Output: DOCS/reports/24-07-2026/planreport_20260724174842.md

### Orchestrator — decisão pós-explorer (acadêmico) + planejamento — 20260724174730
- Summary: Explorer acadêmico validou viabilidade sem bloqueadores (trabalho aditivo). Orquestrador resolveu os itens técnicos; Coordenador confirmou os 3 defaults de produto e pré-autorizou a implementação ("pode implementar").
- Inputs:
  - DOCS/reports/24-07-2026/explorerreport_20260724172653.md
- Decision (Coordenador — 3 defaults):
  1. DOAJ FORA do v1 (só Crossref + Unpaywall; links DOAJ são HTML e morreriam no %PDF).
  2. PDF escaneado: aceitar como está (Opção 1; scan já erra e não indexa — arquivo morto cosmético).
  3. Abstract ausente no Crossref: julgar pelo título com barra extra-conservadora (não buscar abstract externo).
- Decision (Orquestrador — técnicas passadas ao planner):
  1. UM comando Rust `librarian_fetch_and_save_pdf` (baixa→valida %PDF→grava atômico) + helper `write_bytes_atomic` compartilhado; documents_write_ai_source_file (.md) intacto; sem mudança no Cargo.toml.
  2. Dois comandos DeepSeek finos via librarian_decide (`librarian_academic_query`, `librarian_academic_relevance`); cap 3 reforçado no TS.
  3. Crossref+Unpaywall no frontend com teste de CORS no webview + fork documentado para Rust se falhar; strip de JATS-XML no TS.
  4. Estender runPipeline (fase acadêmica sequencial após web); mesma AI folder; inFlightUrls chave `doi:`; savedCount somado; um scan + um onSaved. Zero mudança em useChat/enqueueLibrarian.
  5. Filename `{ano}_{slug}.pdf` sanitizado no TS; dedupe por DOI + filename.
- Decision (Orquestrador — processo): dado o "pode implementar", ao receber o plano, se não houver decisão nova pendente, seguir DIRETO para o codeAgent (sem novo gate de aprovação de plano), reportando o plano ao Coordenador no handoff. Se o planner levantar mudança de escopo, parar e escalar.
- Next step: plannerAgent da extensão disparado. Aguardando plano; em seguida codeAgent (execução) se limpo.

### CodeAgent — implementação inicial (sub-pipeline acadêmico Crossref→Unpaywall→PDF→RAG) — 20260724175833
- Summary: Implementadas as 9 etapas do planreport_20260724174842.md. Fase Rust: (1) extraído helper `write_bytes_atomic(dir, safe_name, &[u8])` do bloco atômico do comando `.md`; `documents_write_ai_source_file` refatorado para chamá-lo (comportamento `.md` inalterado — validações, gate `origin=='ai'`, sanitização e gate `.md` intactos). (2) Novo comando `librarian_fetch_and_save_pdf(folder_id, url, filename)` em documents.rs: espelha resolução `original_path`/`origin` + gate + sanitização (extensão `.pdf`), baixa com `Client::builder().user_agent("Monet/0.1 (mailto:luizvilarinho@gmail.com)").timeout(30s)` (redirect default), lê `.bytes()`, checa `MAX_FILE_BYTES` e `starts_with(b"%PDF")`, grava via helper. Bytes nunca cruzam o IPC. (3) `librarian_academic_query` (field "queries") e `librarian_academic_relevance` (field "dois"; barra extra p/ abstract ausente; "approve at most 3") via `librarian_decide`, em lib.rs ao lado de triage/judge. (4) Os 3 comandos registrados no `generate_handler!` (os 2 finos sem prefixo como triage/judge; o de PDF como `documents::librarian_fetch_and_save_pdf`). Fase TS (librarian.ts): (5) wrappers `librarianAcademicQuery`/`librarianAcademicRelevance`/`librarianFetchAndSavePdf`. (6) `crossrefSearch`/`unpaywallResolve` via `fetch` (Caminho A — CORS confirmado), `stripJats` (regex `<[^>]+>`→'' + colapso de espaço), polite pool só via `mailto=` query param (sem tentar setar User-Agent no fetch); parse tolerante, `[]`/`null` em erro. (7) `academicSlug`/`academicFilename` → `{ano}_{slug}.pdf`. (8) `runPipeline` reestruturado em `runWebPhase` + `runAcademicPhase`: os early-returns do bloco web viraram `return 0` (encerram só a fase); `ensureFolder` memoizado (criação preguiçosa compartilhada); `claimed`/`savedCount` compartilhados; dedupe acadêmico por `doi:`+filename no mesmo `inFlightUrls`; throttle 250ms; UM scan e UM `onSaved(soma)` no fim. Comportamento do pipeline web preservado (mesma dedupe/triagem/extract/julgamento/escrita verbatim/in-flight; só virou função de fase e usa `ensureFolder`).
- Teste de CORS (Etapa 6.0): Caminho A CONFIRMADO. Probe com `Origin: tauri://localhost` retornou HTTP 200 + `Access-Control-Allow-Origin: *` em ambos (Crossref e Unpaywall). Um GET simples de `tauri://localhost` é permitido pelo wildcard. Caminho B (fork Rust) NÃO implementado — nenhum código Rust extra escrito nesta etapa.
- Validação (Etapa 9): `npx tsc --noEmit` exit 0; `cargo check` (src-tauri) exit 0; `npx vite build` exit 0 (avisos pré-existentes de chunk >500kB e de import misto de documents.ts — este último agora também cita librarian.ts pelos dynamic imports, mesmo padrão já existente). Confirmado: `documents_write_ai_source_file` (.md) com comportamento inalterado; useChat.ts/enqueueLibrarian NÃO tocados; Cargo.toml NÃO alterado.
- Desvios do plano: nenhum. Decisões travadas respeitadas integralmente.
- Files changed:
  - src-tauri/src/documents.rs
  - src-tauri/src/lib.rs
  - src/lib/librarian.ts
- Files created:
  - nenhum
- Files removed:
  - nenhum (dead code removido: o bloco atômico inline do comando .md foi substituído pela chamada ao helper compartilhado)
- Left behind: nenhum. Teste manual sugerido pelo plano (Etapa 9) não executado — requer app em execução com chat/pasta e webResearchEnabled; validação automatizada (tsc/vite/cargo) limpa.

### Orchestrator — ajuste pós-teste manual (relevância acadêmica) — 20260724183728
- Summary: Coordenador testou os dois pipelines. Achados: (1) pipeline web excelente — 2 dos 3 .md salvos para "Odisseia + crítica de Platão" eram artigos científicos peer-reviewed on-topic (Brisson/Anais de Filosofia Clássica; Santos/Revista Archai), o 3º um ensaio acadêmico (VoegelinView), zero off-topic. (2) Pipeline acadêmico funciona mecanicamente (achou OA, baixou, validou %PDF, salvou) — o "zero" do 1º teste era do tema (humanidades têm cobertura Crossref/Unpaywall fraca), não bug. (3) PORÉM erro de MIRA DE RELEVÂNCIA: no turno "crítica de Aristóteles a Homero" ele salvou a tese "Homer and the Poetics of Hades" (216pp, 2MB, Durham) — casou no assunto amplo "Homero" + colisão do termo "Poetics" (a tese usa "poetics" em sentido próprio, nada a ver com a *Poética* de Aristóteles), aprovada por julgamento só-por-título (sem abstract). Busca o ASSUNTO, não a PERGUNTA.
- Decision (Coordenador — escopo do ajuste): (a) tuning dos prompts `librarian_academic_query` + `librarian_academic_relevance`, mas OBRIGATORIAMENTE GENÉRICO e simples — proibido qualquer termo específico do caso (Aristóteles/Platão/Homero/Poetics); tem que valer para qualquer tema. Princípio: query captura assunto + recorte específico; juiz aprova só se a fonte trata do recorte, rejeita casamento só-por-título ambíguo e na dúvida rejeita. (b) SEM filtro `journal-article` — Coordenador quer permitir teses/livros longos. (c) Teto de 10MB só no download do bibliotecário (`librarian_fetch_and_save_pdf`), separado do MAX_FILE_BYTES global de 50MB (uploads do usuário). (d) Fix de raiz (juiz ler 1ª página do PDF) SEGURADO — só se o tuning de prompt não bastar em testes futuros. Nota: a tese tem só 2MB, então o teto de 10MB não pega ESTE caso — quem conserta é o prompt de relevância; o teto é guardrail geral.
- Decision (Orquestrador — processo): ajuste pequeno e delimitado → contexto mínimo + codeAgent pontual (sem explorer/planner), conforme regra do papel para ajustes pós-teste.
- Next step: codeAgent do ajuste disparado (edição dos 2 prompts genéricos + teto 10MB). Após conclusão, novo teste manual do Coordenador; se OK, seguir para Etapa 2 (review unificado web+acadêmico).

### CodeAgent — ajuste relevância acadêmica + teto 10MB — 20260724183923
- Summary: Ajuste pontual pós-teste em 3 pontos, todos genéricos (nenhum termo do caso). (1) Prompt de `librarian_academic_query` reescrito para capturar assunto + recorte específico (autor/conceito/relação/claim), com regra explícita de NÃO reduzir ao substantivo dominante e de ancorar em TODAS as entidades quando a pergunta relaciona duas ou mais. (2) Prompt de `librarian_academic_relevance` reescrito para aprovar só quando a fonte trata do recorte específico (não do assunto amplo), rejeitar casamento só-por-keyword/tema, rejeitar título-sem-abstract quando só bate no tema amplo ou é ambíguo, e na dúvida rejeitar; mantidos barra alta e cap de 3. (3) `librarian_fetch_and_save_pdf`: nova constante `LIBRARIAN_MAX_PDF_BYTES = 10 * 1024 * 1024`, checagem antecipada via `resp.content_length()` (descarta sem baixar) + validação sempre do tamanho real dos bytes; substituída a antiga checagem pós-download de `MAX_FILE_BYTES` (que virava inalcançável com o teto de 10MB) pela nova. `MAX_FILE_BYTES` global (50MB) intacto e ainda usado pelo comando `.md` e pelo scan de pasta.
- Validação: `cargo check` (src-tauri) exit 0, limpo. Nenhum arquivo TS tocado (ajuste Rust-only). Confirmado que só os 3 pontos mudaram; pipeline, ordenação, filtros de tipo e comando `.md` intactos.
- Files changed:
  - src-tauri/src/lib.rs
  - src-tauri/src/documents.rs
- Files created:
  - nenhum
- Files removed:
  - nenhum
- Left behind: nenhum. Fix de raiz (juiz ler 1ª página do PDF) permanece segurado pelo Coordenador para caso o tuning de prompt não baste em testes futuros.

### CodeReviewAgent — 20260724190643
- Summary: Review unificado dos dois pipelines do bibliotecário (web + acadêmico) na branch feature/websearch-rag, via diff main...HEAD. Correção, atomicidade, gate por flag, verbatim, single-round (fix bug #3), sanitização/path-traversal, gate origin='ai', parsing tolerante Rust, liberação do inFlightUrls, serialização por pasta e disparo único do pipeline: todos verificados corretos. 2 achados [importante], ambos gaps de dedupe SEM corrupção e com auto-correção: (1) inFlightUrls global chaveado só por filename/doi → falso-dedupe cross-folder (pasta B pode perder uma gravação legítima quando pasta A processa a mesma URL concorrentemente); (2) runPipeline localiza a AI folder pelo snapshot de visibleDocumentIds (defasado vs onFolderRegistered) → extract redundante + toast inflado nos turnos iniciais antes da pasta propagar. 4 nits (fases não isoladas contra exceção; failOnAssistant não limpa librarianContextRef — safe; colisão de slug acadêmico; SSRF baixo risco no download PDF) + 1 informacional (e-mail pessoal hardcoded, decisão travada). Sem críticos. Decisões travadas respeitadas (não reportadas como defeito).
- Verdict: APROVADO COM RESSALVAS
- Output: DOCS/reports/24-07-2026/reviewreport_20260724190643.md

### Orchestrator — validação do ajuste + abertura da Etapa 2 (review) — 20260724190023
- Summary: Coordenador testou de novo ("crítica de Platão, Aristóteles, Xenófanes e Calímaco a Homero, em especial a Odisseia"). Resultados avaliados pelo Orquestrador (leitura dos arquivos gravados): 7 .md novos, todos on-topic (4 Xenófanes + 3 Calímaco), zero off-topic; 3 são artigos peer-reviewed (Costa/Ci.Huma.e Soc.; Gomes/Revista Pandora; Clausen/GRBS-Duke), 2 de referência (Wikipedia, Dickinson), 2 blogs populares (Substack, TheCollector). O ajuste FUNCIONOU: nenhum PDF tangencial entrou (pipeline acadêmico salvou 0 = comportamento correto agora; juiz mais rígido segurou o caso tipo "tese do Hades"). Gap conhecido persiste (não é regressão): em humanidades o valor vem do pipeline web, que inclusive captura PDFs OA de journals como .md; o pipeline acadêmico Crossref/Unpaywall subentrega nesse domínio. Trade-off "raw sem limpeza" visível (lixo de nav no topo de alguns .md) — aceito. Coordenador decidiu USAR ASSIM por algumas semanas antes de novas alterações e autorizou o review.
- Decision (Coordenador): desenvolvimento OK — prosseguir para Etapa 2 (review unificado web + acadêmico). Nenhum ajuste adicional agora (nudge opcional de "preferir fontes acadêmicas" no juiz web NÃO aplicado — fica para eventual rodada futura).
- Next step: codereviewAgent disparado sobre toda a feature (remoção da coreografia + pipeline web + extensão acadêmica). Ao receber o relatório: atualizar ## Documents, spawnar UM codeAgent para ler o review, decidir ajustes e aplicar a passada final, e então encerrar a task reportando ao Coordenador.

### CodeAgent — passada final pós-review — 20260724191413
- Summary: Lidos o review report (reviewreport_20260724190643.md) e o histórico do Execution Report. Aplicados os 2 achados [importante] (bugs reais de dedupe) + 2 nits de baixo risco; 2 nits e 1 informacional conscientemente deixados de fora. Nenhuma feature nova; decisões travadas respeitadas integralmente. Sem git.
- Aplicado:
  - **[importante] 1 — Falso-dedupe cross-folder (`librarian.ts`):** as chaves do `inFlightUrls` global passaram a ser compostas com `chatFolderId`. Web: `${chatFolderId}:${filename}`. Acadêmico: `${chatFolderId}:${filename}` (fileKey) e `${chatFolderId}:doi:${doi}` (doiKey). O `claimed[]` guarda as chaves compostas, então o release no `finally` continua consistente. Duas pastas processando a MESMA URL concorrentemente não se falso-deduplicam mais — cada uma grava na sua própria KB. Comentário do bloco in-flight atualizado para descrever o keying por pasta.
  - **[importante] 2 — Gap de dedupe na criação da AI folder (`librarian.ts` runPipeline):** a localização da AI folder existente deixou de depender do snapshot defasado de `input.visibleDocumentIds` e passou a ser pelo caminho determinístico — `d.docType==='folder' && d.origin==='ai' && originalPath` (normalizado `\`→`/`) `endsWith('web-research/<chatFolderId>')`, espelhando `documents_ensure_ai_folder` (que resolve por `original_path`). Elimina o re-extract Tavily redundante + toast inflado nos turnos iniciais antes do id propagar ao estado React. Campo `visibleDocumentIds` ficou órfão: removido do `LibrarianInput` (librarian.ts), do tipo do `librarianContextRef`, dos 2 sites de população (caminho sem tools + branch web_search) e da chamada `enqueueLibrarian` (useChat.ts). Dead code removido.
  - **[nit] 3 — Isolamento de fase contra exceção (`librarian.ts` runPipeline):** `runWebPhase` e `runAcademicPhase` agora rodam cada uma em seu próprio try/catch (log em `console.warn`), coerente com a intenção "complementar" — uma exceção numa fase não impede a outra. `savedCount` continua a soma das duas; o `finally` externo (release do in-flight) intacto.
  - **[nit] 4 — `failOnAssistant` limpa `librarianContextRef` (`useChat.ts`):** adicionada `librarianContextRef.current = null`, alinhando os três caminhos de término (error/cancel/failOnAssistant). Defensivo e trivial.
- Deixado de fora (conscientemente):
  - **[nit] 5 — Colisão de slug acadêmico:** NÃO aplicado (orientação do Orquestrador). Baixa probabilidade, coerente com o modo oportunista; adicionar disambiguação (ex.: hash do DOI no filename) agregaria complexidade sem requisito real.
  - **[nit] 6 — SSRF sem bloqueio de IP privado no download do PDF:** NÃO aplicado (orientação do Orquestrador). App desktop local, fonte de URL reputável (Unpaywall), nenhum corpo devolvido ao caller, timeout+teto 10MB+validação `%PDF` já presentes; bloquear faixas privadas agregaria complexidade de rede desproporcional ao risco. Adiado.
  - **[informacional] 7 — E-mail pessoal hardcoded:** mantido como está por DECISÃO TRAVADA do Coordenador (produto). Não é defeito; só registrado.
- Validação: `npx tsc --noEmit` exit 0 (limpo). `npx vite build` exit 0 (só avisos pré-existentes: chunk >500kB e import misto de documents.ts — este já citava librarian.ts pelos dynamic imports). `cargo check` NÃO executado — passada TS-only, nenhum arquivo Rust tocado. Comportamento do pipeline web (dedupe/triagem/extract/julgamento/escrita verbatim) e o comando `.md` (`documents_write_ai_source_file`) permanecem intactos — nenhuma dessas rotinas foi editada.
- Files changed:
  - src/lib/librarian.ts
  - src/hooks/useChat.ts
- Files created:
  - nenhum
- Files removed:
  - nenhum (campo `visibleDocumentIds` órfão removido do LibrarianInput/ref/sites de população/chamada — dead code eliminado nos arquivos existentes)
- Left behind: nits 5 (colisão de slug) e 6 (SSRF IP privado) conscientemente adiados por baixo risco/complexidade desproporcional; informacional 7 (e-mail fixo) mantido por decisão travada. Fix-de-raiz do juiz acadêmico (ler 1ª página do PDF) segue segurado pelo Coordenador. Nenhuma pendência de correção.

### Orchestrator — encerramento — 20260724191413
- Summary: Task concluída. Feature "bibliotecário" entregue em dois pipelines complementares, orquestrados por código em background: (1) WEB — substitui a coreografia de tool-calling multi-rodada (fonte de 5 bugs) por dedupe→triagem→extract→julgamento→escrita verbatim, disparado em onOpenRouterDone; (2) ACADÊMICO — Crossref→Unpaywall→download de PDF OA→validação %PDF→RAG. Fluxo completo executado: Task Report (web) → explorer → planner → code; Task Report (acadêmico, expansão de escopo aprovada) → explorer → planner → code; 1 ajuste pós-teste (relevância acadêmica genérica + teto 10MB); Etapa 2 review → passada final. Validação final: tsc/vite/cargo limpos ao longo do fluxo; grep de resíduos da coreografia = 0.
- Review: APROVADO COM RESSALVAS (0 críticos, 2 importantes, 4 nits, 1 informacional). Os 2 importantes (falso-dedupe cross-folder; gap de dedupe na criação da AI folder) corrigidos na passada final. Nits 3/4 aplicados; nits 5/6 e informacional 7 conscientemente deixados de fora (ver sessão acima).
- Deixado para trás (após análise do review): nit 5 (colisão de slug acadêmico) e nit 6 (SSRF sem bloqueio de IP privado no download) — baixo risco, complexidade desproporcional; informacional 7 (e-mail pessoal hardcoded em Crossref/Unpaywall) — decisão travada do Coordenador, mas com implicação registrada para build distribuído (Microsoft Store). Fix-de-raiz do juiz acadêmico (juiz ler 1ª página do PDF) segurado pelo Coordenador para caso o tuning de prompt não baste em uso real (semanas de teste). Gap conhecido de produto: em humanidades o pipeline acadêmico Crossref/Unpaywall subentrega (cobertura OA fraca) — o valor vem do pipeline web; não é regressão.
- Decision: task encerrada. Sem git por decisão do Coordenador (trabalho fica na branch feature/websearch-rag). Coordenador vai usar a feature por algumas semanas antes de novas alterações.
- Next step: concluído.
