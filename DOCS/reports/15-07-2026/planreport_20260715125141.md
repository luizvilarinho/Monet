# Plano

## Escopo
Quatro melhorias incrementais no Reader (`src/components/Reader/Reader.tsx` + `src/components/Reader/Reader.module.css`, únicos arquivos tocados — sem mudanças de schema, `Storage`, ou `src/types`):

1. Filtro por cor no painel de highlights.
2. Troca de cor de highlight existente via popover (reaproveitando `persistHighlight`/`saveHighlight`, que já é upsert).
3. Botões flutuantes de navegação de página (`.pageArea`, position absolute, sem ocupar altura).
4. Busca textual no ebook (palavra/frase), com indexação do documento inteiro em background ao abrir a busca (decisão do Coordenador — não incremental/sob demanda), contagem de ocorrências, navegação next/previous, e destaque visual via `Range.getClientRects()` só computável na página atualmente renderizada.

Ordem de execução: 1→2→3 primeiro (baixo risco, isolados, confirm估do pelo explorer report), depois a busca (4) como bloco próprio dividido em 6 etapas menores, por ser a única com lógica nova relevante.

**Limitações aceitas conscientemente (não implementar solução mais genérica)**:
- Busca é case-insensitive mas **não** accent-insensitive (`"cafe"` não casa com `"café"`) — normalização de diacríticos não foi pedida no task report, só citada como edge case a conhecer.
- A contagem por página (índice, baseado em `getPageText`) e os retângulos desenhados na página atual (baseados na text layer ao vivo) usam o mesmo algoritmo de junção (`join(' ')` entre itens/spans), então convergem no caso comum (busca de uma palavra). Frases que cruzam múltiplos spans do pdf.js podem, em casos raros, divergir em 1-2 ocorrências entre a contagem e o destaque. Não vale a pena unificar perfeitamente os dois caminhos para esse caso raro.

**Ajustes pós-review (passada final do codeAgent, 2026-07-15)**:
O `codereviewAgent` aprovou a implementação e reportou 3 nits não-bloqueantes. Um foi corrigido (aria-label dos swatches de cor sem diferenciação por cor — agora usa nome legível via novo mapa `HIGHLIGHT_COLOR_NAMES`). Os outros dois foram conscientemente deixados como estão:
- Recomputo O(P²) de `matchesByPage` durante a indexação em background: só relevante para documentos muito grandes (500+ páginas) com uma busca já digitada durante a indexação; corrigir exigiria trocar o `useMemo` puro por um acumulador incremental (ref + reducer), adicionando complexidade de invalidação de cache a uma lógica já correta e aprovada, para um ganho perceptível apenas num caso extremo.
- Risco de overflow da toolbar em janelas estreitas com busca aberta: padrão de layout pré-existente (`.toolbar` já não tinha `flex-wrap` antes desta task), não é uma regressão introduzida por este diff. Corrigir tocaria CSS compartilhado da toolbar fora do escopo desta task incremental.

## Legenda de status das etapas
- `PENDENTE` — etapa ainda não iniciada
- `FEITO` — etapa concluída sem ressalvas
- `REVISAR` — etapa executada, mas ainda depende de ajuste

---

## Etapas

### Etapa 1 — Filtro por cor no painel de highlights (Requisito 2)
- Status: FEITO
- Descrição:
  1. Adicionar estado `const [colorFilter, setColorFilter] = useState<string | null>(null)` junto aos demais estados de highlight (perto de `activeColor`, linha ~282).
  2. No effect que carrega highlights ao trocar de livro (linhas 409-423), adicionar `setColorFilter(null)` junto a `setHighlights([])`/`setSelection(null)`/`setRemovalCandidate(null)` — o filtro não deve sobreviver à troca de livro.
  3. Adicionar um `useMemo` logo após `orderedHighlights` (linhas 910-916):
     ```ts
     const filteredHighlights = useMemo(
       () => (colorFilter ? orderedHighlights.filter((h) => h.color === colorFilter) : orderedHighlights),
       [orderedHighlights, colorFilter],
     )
     ```
  4. No JSX do painel "Highlights" (linhas 1457-1546), trocar a condição atual `orderedHighlights.length === 0 ? (<p empty/>) : (<ul>...</ul>)` por uma estrutura com 3 casos, envolvidos num fragment `<>`:
     - `orderedHighlights.length === 0` → `<p className={styles.highlightsPanelEmpty}>No highlights yet</p>` (comportamento atual, inalterado).
     - `filteredHighlights.length === 0` (mas `orderedHighlights.length > 0`) → `<p className={styles.highlightsPanelEmpty}>No highlights match this color</p>`.
     - Caso contrário → o `<ul className={styles.highlightsPanelList}>` atual, mas iterando `filteredHighlights.map(...)` em vez de `orderedHighlights.map(...)` (só essa troca — o conteúdo de cada `<li>` não muda nesta etapa).
     Depois do bloco condicional acima (ainda dentro do mesmo fragment, como irmão — não dentro da `<ul>`/`<p>`), renderizar a fileira de swatches **somente quando `orderedHighlights.length > 0`**:
     ```tsx
     {orderedHighlights.length > 0 && (
       <div className={styles.highlightsColorFilter}>
         {HIGHLIGHT_COLORS.map((c) => (
           <button
             key={c}
             type="button"
             className={`${styles.highlightSwatch} ${c === colorFilter ? styles.highlightSwatchActive : ''}`}
             style={{ background: c }}
             onClick={() => setColorFilter((prev) => (prev === c ? null : c))}
             aria-pressed={c === colorFilter}
             aria-label={`Filter highlights by color`}
             title="Filter by color"
           />
         ))}
       </div>
     )}
     ```
     Clicar numa cor já selecionada desmarca o filtro (volta a mostrar todos) — é exatamente o que `prev === c ? null : c` faz.
  5. CSS: em `Reader.module.css`, inserir depois de `.highlightsPanelEmpty` (linha 769) e antes do comentário "Toast de confirmação" (linha 771):
     ```css
     /* ─── Filtro de cor no painel de grifos ──────────────────────────────── */
     .highlightsColorFilter {
       display: flex;
       align-items: center;
       justify-content: center;
       gap: 10px;
       padding: 10px 8px;
       border-top: 1px solid var(--border, #333340);
       flex-shrink: 0;
     }
     ```
     Reaproveita `.highlightSwatch`/`.highlightSwatchActive` já existentes (linhas 281-298) — não criar novas classes de swatch.
- Arquivos: `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- Depende de: -

---

### Etapa 2 — Trocar cor de highlight existente via popover (Requisito 3)
- Status: FEITO
- Descrição:
  1. Adicionar uma função perto de `persistHighlight`/`removeHighlight` (linhas 918-943):
     ```ts
     function handleChangeHighlightColor(h: BookHighlight, color: string) {
       setRemovalCandidate(null)
       if (color === h.color) return
       void persistHighlight({ ...h, color })
     }
     ```
     Fecha o popover imediatamente ao escolher uma cor (mesmo padrão das quick actions "Ask AI"/"Copy citation" do próprio popover, que também chamam `setRemovalCandidate(null)`). Não criar nenhuma função nova de storage — `persistHighlight` já faz upsert local + `storage.saveHighlight`.
  2. No JSX do `removalPopover` (linhas 1630-1689), inserir uma nova linha de swatches **entre** o fechamento de `.removalPopoverQuickActions` (`</div>`, linha ~1669) e a abertura de `.removalPopoverActions` (linha ~1670):
     ```tsx
     <div className={styles.removalPopoverColors}>
       {HIGHLIGHT_COLORS.map((c) => (
         <button
           key={c}
           type="button"
           className={`${styles.highlightSwatch} ${
             c === removalCandidate.highlight.color ? styles.highlightSwatchActive : ''
           }`}
           style={{ background: c }}
           onClick={() => handleChangeHighlightColor(removalCandidate.highlight, c)}
           aria-label={`Change highlight color`}
           title="Change color"
         />
       ))}
     </div>
     ```
     Não adicionar nenhum texto/label acima da fileira (mesmo padrão "sem label" já usado na toolbar de seleção com `HIGHLIGHT_COLORS`).
  3. CSS: em `Reader.module.css`, inserir depois de `.removalPopoverQuickButton:hover` (linha 369) e antes de `.removalPopoverActions` (linha 371):
     ```css
     .removalPopoverColors {
       display: flex;
       align-items: center;
       gap: 6px;
       padding: 2px 0;
     }
     ```
  4. Edge case "nota vinculada": não requer tratamento — confirmado pelo explorer report que `BookHighlight` não referencia `Note`/`BookQuote`, então trocar a cor não afeta nenhuma nota.
- Arquivos: `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- Depende de: -

---

### Etapa 3 — Botões flutuantes de navegação de página (Requisito 4)
- Status: FEITO
- Descrição:
  1. Adicionar `CaretLeft, CaretRight` ao import de `@phosphor-icons/react` no topo do arquivo (linhas 3-10), mantendo ordem alfabética com os demais ícones já importados.
  2. No JSX de `.pageArea` (linhas 1549-1559 até o fechamento em 1690-1692), adicionar, como **irmão** do `{loading ? ... : <div className={pageWrap}>...}` (dentro de `.pageArea`, fora do `.pageWrap`), um bloco condicional a `doc` (não a `!loading` isoladamente — usar `doc` garante que os botões só aparecem quando o documento carregou, consistente com o resto da UI que usa `doc` para habilitar/desabilitar):
     ```tsx
     {doc && (
       <>
         <button
           type="button"
           className={`${styles.floatingNavButton} ${styles.floatingNavButtonLeft}`}
           onClick={() => goToPage(pageNum - 1)}
           disabled={pageNum <= 1}
           aria-label="Previous page"
         >
           <CaretLeft size={18} aria-hidden />
         </button>
         <button
           type="button"
           className={`${styles.floatingNavButton} ${styles.floatingNavButtonRight}`}
           onClick={() => goToPage(pageNum + 1)}
           disabled={pageNum >= doc.numPages}
           aria-label="Next page"
         >
           <CaretRight size={18} aria-hidden />
         </button>
       </>
     )}
     ```
     Mesma lógica de `disabled` dos botões do topo (linhas 1280-1313): `pageNum <= 1` / `pageNum >= doc.numPages`. Reaproveita `goToPage` — não criar nova função de navegação.
  3. CSS em `Reader.module.css`:
     - Modificar a regra `.pageArea` existente (linhas 97-105) adicionando `position: relative;` (necessário para os botões absolutos usarem `.pageArea` como containing block — o padding box de um ancestral posicionado não se move ao rolar o conteúdo interno, então os botões ficam fixos visualmente na parte inferior mesmo com scroll).
     - Inserir depois de `.canvas` (linha 124) e antes de `.loadingText` (linha 126):
       ```css
       /* ─── Botões flutuantes de navegação de página ──────────────────────────
          Filhos diretos de .pageArea (não de .pageWrap): como .pageArea é o
          ancestral posicionado (position: relative) E o scroll container, os
          botões absolutos ficam fixos na parte inferior VISÍVEL mesmo quando
          o conteúdo rola (não acompanham o scroll), sem ocupar altura no
          layout. */
       .floatingNavButton {
         position: absolute;
         bottom: 20px;
         z-index: 5;
         width: 40px;
         height: 40px;
         border-radius: 50%;
         display: inline-flex;
         align-items: center;
         justify-content: center;
         background: var(--bg-panel, #161625);
         border: 1px solid var(--border, #333340);
         color: var(--text-primary, #ddddf1);
         cursor: pointer;
         opacity: 0.55;
         transition: opacity 0.15s ease, border-color 0.15s ease;
       }

       .floatingNavButton:hover:not(:disabled) {
         opacity: 1;
         border-color: var(--accent, #7b6ed4);
       }

       .floatingNavButton:disabled {
         opacity: 0.2;
         cursor: not-allowed;
       }

       .floatingNavButtonLeft {
         left: 20px;
       }

       .floatingNavButtonRight {
         right: 20px;
       }
       ```
  4. Não mexer nos botões `‹`/`›` existentes no topo (linhas 1280-1313) — devem continuar exatamente como estão.
- Arquivos: `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- Depende de: -

---

### Etapa 4 — Busca textual: estado, tipos e helpers (Requisito 1, parte 1/6)
- Status: FEITO
- Descrição:
  1. Adicionar ao import de `@phosphor-icons/react` (linhas 3-10): `CaretDown, CaretUp, MagnifyingGlass, X` (mantendo ordem alfabética junto com `CaretLeft`/`CaretRight` já adicionados na Etapa 3).
  2. Perto das interfaces `PagePoint`/`SelectionState`/`RemovalCandidate` (linhas 66-82), adicionar:
     ```ts
     interface SearchMatch {
       page: number
       occurrenceIndexOnPage: number
     }

     interface PageMatchCount {
       page: number
       count: number
     }
     ```
  3. Perto dos helpers de DOM já existentes (`findSpanElement`, `getCaretFromPoint`, linhas 224-256), adicionar dois helpers puros de módulo (fora do componente):
     ```ts
     // Reconstrói o texto de uma página a partir dos <span> renderizados pela
     // TextLayer do pdfjs, na mesma ordem em que são renderizados (1 span por
     // item de texto). Junta com um único espaço entre spans — MESMA
     // convenção usada por getPageText (`.map(item => item.str).join(' ')`),
     // para que a busca no DOM ao vivo encontre os mesmos termos que a
     // indexação em background encontrou no texto extraído.
     function getPageSpans(container: HTMLElement): HTMLElement[] {
       return Array.from(container.querySelectorAll<HTMLElement>('span')).filter(
         (s) => (s.textContent ?? '').length > 0,
       )
     }

     interface SpanRange {
       span: HTMLElement
       start: number
       end: number
     }

     function buildJoinedPageText(spans: HTMLElement[]): {
       text: string
       spanRanges: SpanRange[]
     } {
       let text = ''
       const spanRanges: SpanRange[] = []
       spans.forEach((span, i) => {
         const content = span.textContent ?? ''
         const start = text.length
         text += content
         spanRanges.push({ span, start, end: text.length })
         if (i < spans.length - 1) text += ' '
       })
       return { text, spanRanges }
     }

     // Localiza todas as ocorrências (case-insensitive) de `query` no texto
     // reconstruído da text layer e devolve um Range por ocorrência,
     // construído a partir dos spans que a ocorrência atravessa. Uma
     // ocorrência pode cruzar mais de um span (frase); nesse caso o Range
     // vai do offset inicial no primeiro span ao offset final no último.
     function findAllOccurrenceRanges(container: HTMLElement, query: string): Range[] {
       if (!query) return []
       const spans = getPageSpans(container)
       if (spans.length === 0) return []
       const { text, spanRanges } = buildJoinedPageText(spans)
       const lower = text.toLowerCase()
       const q = query.toLowerCase()
       const ranges: Range[] = []
       let searchFrom = 0
       let idx = lower.indexOf(q, searchFrom)
       while (idx !== -1) {
         const range = buildRangeFromOffsets(spanRanges, idx, idx + q.length)
         if (range) ranges.push(range)
         searchFrom = idx + q.length
         idx = lower.indexOf(q, searchFrom)
       }
       return ranges
     }

     function buildRangeFromOffsets(
       spanRanges: SpanRange[],
       start: number,
       end: number,
     ): Range | null {
       let startInfo: { span: HTMLElement; offset: number } | null = null
       let endInfo: { span: HTMLElement; offset: number } | null = null
       for (const sr of spanRanges) {
         if (!startInfo && start < sr.end) {
           startInfo = { span: sr.span, offset: start - sr.start }
         }
         if (!endInfo && end <= sr.end) {
           endInfo = { span: sr.span, offset: end - sr.start }
           break
         }
       }
       if (!startInfo || !endInfo) return null
       const startTextNode = startInfo.span.firstChild
       const endTextNode = endInfo.span.firstChild
       if (!startTextNode || !endTextNode) return null
       try {
         const range = document.createRange()
         range.setStart(startTextNode, Math.max(0, startInfo.offset))
         range.setEnd(endTextNode, Math.max(0, endInfo.offset))
         return range
       } catch {
         return null
       }
     }
     ```
     Nota: cada `<span>` da text layer do pdfjs contém um único text node filho — `span.firstChild` é seguro aqui (mesma suposição implícita já usada por `findSpanElement`/`getCaretFromPoint` no controlador de seleção manual existente).
  4. Dentro do componente `Reader`, perto dos demais estados de busca a adicionar nesta e nas próximas etapas, declarar:
     ```ts
     const [searchOpen, setSearchOpen] = useState(false)
     const [searchQuery, setSearchQuery] = useState('')
     const [searchIndexReady, setSearchIndexReady] = useState(false)
     const [searchIndexProgress, setSearchIndexProgress] = useState(0)
     const [activeMatchIndex, setActiveMatchIndex] = useState(-1)
     const [pageMatchRects, setPageMatchRects] = useState<BookHighlightRect[][]>([])
     const searchIndexingRef = useRef(false)
     ```
     `activeMatchIndex === -1` significa "nenhuma ocorrência navegada ainda para esta busca" (distinto de "na primeira ocorrência").
  5. Adicionar os memos derivados, logo após `orderedHighlights`/`filteredHighlights` (Etapa 1):
     ```ts
     const normalizedQuery = searchQuery.trim().toLowerCase()

     const matchesByPage = useMemo<PageMatchCount[]>(() => {
       if (!normalizedQuery) return []
       const result: PageMatchCount[] = []
       for (let n = 1; n <= searchIndexProgress; n++) {
         const text = pageTextCacheRef.current.get(n)
         if (!text) continue
         const lower = text.toLowerCase()
         let count = 0
         let idx = lower.indexOf(normalizedQuery)
         while (idx !== -1) {
           count++
           idx = lower.indexOf(normalizedQuery, idx + normalizedQuery.length)
         }
         if (count > 0) result.push({ page: n, count })
       }
       return result
     }, [normalizedQuery, searchIndexProgress])

     const flatMatches = useMemo<SearchMatch[]>(() => {
       const flat: SearchMatch[] = []
       for (const pm of matchesByPage) {
         for (let i = 0; i < pm.count; i++) {
           flat.push({ page: pm.page, occurrenceIndexOnPage: i })
         }
       }
       return flat
     }, [matchesByPage])

     const currentMatch = activeMatchIndex >= 0 ? (flatMatches[activeMatchIndex] ?? null) : null
     ```
     `matchesByPage` lê `pageTextCacheRef.current` (um ref) dentro de um `useMemo` cuja dependência `searchIndexProgress` é atualizada exatamente quando uma nova página termina de ser indexada (Etapa 5) — é um padrão intencional de "ref como cache, state como versão/gatilho de recomputo", não um bug de reatividade.
- Arquivos: `src/components/Reader/Reader.tsx`
- Depende de: -

---

### Etapa 5 — Indexação em background do texto do documento (Requisito 1, parte 2/6)
- Status: FEITO
- Descrição:
  1. Adicionar um `useEffect` que reseta o índice ao trocar de documento — perto do effect existente "Cache de texto por página é do documento atual; zera ao (re)carregar" (linhas 461-463):
     ```ts
     useEffect(() => {
       setSearchIndexReady(false)
       setSearchIndexProgress(0)
     }, [doc])
     ```
  2. Adicionar o effect de indexação em background, logo após `getPageText` (linhas 468-491):
     ```ts
     // Indexação full-document em background: dispara quando a busca é
     // aberta pela primeira vez neste documento, varre TODAS as páginas via
     // getPageText (que já cacheia em pageTextCacheRef) e cede a thread a
     // cada página (setTimeout 0) para nunca travar a UI/render da página
     // atual. Fechar a busca antes de terminar cancela o loop (cleanup),
     // mas reabrir retoma quase instantaneamente pelas páginas já
     // cacheadas — não é necessário persistir um ponteiro de progresso à
     // parte.
     useEffect(() => {
       if (!searchOpen || !doc) return
       if (searchIndexReady || searchIndexingRef.current) return
       searchIndexingRef.current = true
       let cancelled = false
       void (async () => {
         for (let n = 1; n <= doc.numPages; n++) {
           if (cancelled) return
           await getPageText(n)
           if (cancelled) return
           setSearchIndexProgress(n)
           await new Promise((resolve) => setTimeout(resolve, 0))
         }
         if (!cancelled) setSearchIndexReady(true)
       })()
       return () => {
         cancelled = true
         searchIndexingRef.current = false
       }
     }, [searchOpen, doc, searchIndexReady, getPageText])
     ```
  3. No mesmo effect que reseta `selection`/`removalCandidate` ao trocar de livro (linhas 409-423 — já tocado na Etapa 1 para `colorFilter`), adicionar também `setSearchOpen(false)`, `setSearchQuery('')`, `setActiveMatchIndex(-1)`: a busca não deve sobreviver à troca de livro.
- Arquivos: `src/components/Reader/Reader.tsx`
- Depende de: Etapa 4

---

### Etapa 6 — Retângulos de destaque na página atual (Requisito 1, parte 3/6)
- Status: FEITO
- Descrição:
  1. Adicionar uma função (via `useCallback`) que recalcula `pageMatchRects` para a página **atualmente renderizada**, perto de `handleTextMouseUp` (linhas 960-1012) — a lógica de conversão de `Range.getClientRects()` para retângulos normalizados é a MESMA já usada lá (reaproveitar o padrão, não uma abstração nova):
     ```ts
     const computeSearchMatchRects = useCallback(() => {
       const textContainer = textLayerRef.current
       if (!textContainer || !searchOpen || !normalizedQuery) {
         setPageMatchRects([])
         return
       }
       const textRect = textContainer.getBoundingClientRect()
       if (textRect.width === 0 || textRect.height === 0) {
         setPageMatchRects([])
         return
       }
       const ranges = findAllOccurrenceRanges(textContainer, normalizedQuery)
       const rectsPerMatch: BookHighlightRect[][] = ranges.map((range) => {
         const domRects = Array.from(range.getClientRects())
         const rects: BookHighlightRect[] = []
         for (const r of domRects) {
           if (r.width === 0 || r.height === 0) continue
           rects.push({
             x: (r.left - textRect.left) / textRect.width,
             y: (r.top - textRect.top) / textRect.height,
             w: r.width / textRect.width,
             h: r.height / textRect.height,
           })
         }
         return rects
       })
       setPageMatchRects(rectsPerMatch)
     }, [searchOpen, normalizedQuery])
     ```
  2. Seguindo o MESMO padrão já usado no arquivo para `bookRef`/`onBookChangeRef`/etc. (linhas 352-363: `const xRef = useRef(x); xRef.current = x`), adicionar:
     ```ts
     const computeSearchMatchRectsRef = useRef(computeSearchMatchRects)
     computeSearchMatchRectsRef.current = computeSearchMatchRects
     ```
     (declarar isso logo após a definição de `computeSearchMatchRects`, executado a cada render).
  3. Dentro do effect de renderização de página (linhas 494-753), logo depois do bloco `try { const textLayer = new TextLayer(...); await textLayer.render() } catch {...}` (linha ~552, ANTES do cálculo de `lineHeightRef` que já existe em seguida), adicionar uma chamada:
     ```ts
     computeSearchMatchRectsRef.current()
     ```
     Usar o `.current` via ref (não adicionar `searchOpen`/`normalizedQuery` às deps `[doc, pageNum, scale]` desse effect) — isso evita que digitar na busca dispare um re-render do canvas/text layer, que é caro e desnecessário.
  4. Adicionar um effect separado, mais leve, para recalcular quando o usuário digita/abre/fecha a busca **sem trocar de página** (a mudança de página já é coberta pelo passo 3 acima; este effect cobre "ficar na mesma página e mudar a query"):
     ```ts
     useEffect(() => {
       computeSearchMatchRects()
     }, [searchOpen, normalizedQuery, pageNum, computeSearchMatchRects])
     ```
     Incluir `pageNum` aqui é redundante com o passo 3 (ambos podem rodar na troca de página), mas é inofensivo — `computeSearchMatchRects` é idempotente para o mesmo estado do DOM, e o passo 3 (que roda depois de o `await textLayer.render()` da NOVA página terminar) sempre "vence" por rodar mais tarde, corrigindo qualquer cálculo prematuro feito por este effect antes da text layer da nova página estar pronta.
  5. No effect existente que já limpa `selection`/`removalCandidate` ao trocar de página/zoom (linhas 858-862, `useEffect(() => { setSelection(null); setRemovalCandidate(null) }, [pageNum, scale])`), adicionar também `setPageMatchRects([])` — isto evita um flash visual de retângulos da página ANTERIOR (com coordenadas erradas) sendo desenhados sobre a página nova enquanto o novo cálculo (passo 3) ainda não terminou.
- Arquivos: `src/components/Reader/Reader.tsx`
- Depende de: Etapa 4

---

### Etapa 7 — Navegação entre ocorrências (Requisito 1, parte 4/6)
- Status: FEITO
- Descrição:
  1. Adicionar, perto de `goToPage`/`jumpToPage` (linhas 834-850):
     ```ts
     function goToMatch(index: number) {
       const m = flatMatches[index]
       if (!m) return
       setActiveMatchIndex(index)
       goToPage(m.page)
     }

     function goToNextMatch() {
       if (flatMatches.length === 0) return
       const next = activeMatchIndex < 0 ? 0 : (activeMatchIndex + 1) % flatMatches.length
       goToMatch(next)
     }

     function goToPreviousMatch() {
       if (flatMatches.length === 0) return
       const prev =
         activeMatchIndex < 0
           ? flatMatches.length - 1
           : (activeMatchIndex - 1 + flatMatches.length) % flatMatches.length
       goToMatch(prev)
     }
     ```
     `activeMatchIndex < 0` (estado inicial "ainda não naveguei") faz o primeiro Next ir para a ocorrência 0 (não para a 1) e o primeiro Previous ir para a última — comportamento esperado de um find-bar.
  2. Adicionar um effect que reseta `activeMatchIndex` para `-1` sempre que a query muda (nova busca invalida a posição de navegação anterior):
     ```ts
     useEffect(() => {
       setActiveMatchIndex(-1)
     }, [normalizedQuery])
     ```
  3. Adicionar scroll automático até a ocorrência ativa quando ela está na página atualmente renderizada. Isto exige marcar o retângulo ativo no DOM com um atributo (não usar seletor de classe CSS-module, que é frágil) — ver Etapa 9 para o atributo `data-match-active` no JSX dos retângulos. O effect de scroll:
     ```ts
     useEffect(() => {
       if (!currentMatch || currentMatch.page !== pageNum) return
       const el = pageWrapRef.current?.querySelector<HTMLElement>('[data-match-active="true"]')
       el?.scrollIntoView({ block: 'center', inline: 'nearest' })
     }, [currentMatch, pageMatchRects, pageNum])
     ```
- Arquivos: `src/components/Reader/Reader.tsx`
- Depende de: Etapa 4, Etapa 6 (usa `pageMatchRects`), Etapa 9 (usa o atributo `data-match-active` — pode ser implementado em conjunto com a Etapa 9 se for mais prático para o codeAgent seguir a ordem numérica estritamente; o atributo em si deve existir antes deste effect fazer sentido em runtime)

---

### Etapa 8 — UI da busca na toolbar (Requisito 1, parte 5/6)
- Status: FEITO
- Descrição:
  1. No JSX da toolbar (dentro de `.toolbar`, linhas 1272-1359), inserir um novo bloco condicional logo depois do fechamento de `.pageControls` (`</div>`, linha 1314) e antes do bloco condicional `{jumpBackPage !== null && (...)}` (linha 1316):
     ```tsx
     {!searchOpen ? (
       <button
         type="button"
         className={styles.highlightsToggle}
         onClick={() => setSearchOpen(true)}
         aria-label="Search in book"
       >
         <MagnifyingGlass size={14} aria-hidden />
         Search
       </button>
     ) : (
       <div className={styles.searchBar}>
         <input
           type="text"
           className={styles.searchInput}
           placeholder="Search in book…"
           value={searchQuery}
           onChange={(e) => setSearchQuery(e.target.value)}
           onKeyDown={(e) => {
             if (e.key === 'Enter') {
               if (e.shiftKey) goToPreviousMatch()
               else goToNextMatch()
             } else if (e.key === 'Escape') {
               setSearchOpen(false)
             }
           }}
           aria-label="Search query"
           autoFocus
         />
         <span className={styles.searchCount}>
           {!normalizedQuery
             ? ''
             : flatMatches.length === 0
               ? searchIndexReady
                 ? 'No results'
                 : 'Searching…'
               : activeMatchIndex < 0
                 ? `${flatMatches.length} result${flatMatches.length === 1 ? '' : 's'}`
                 : `${activeMatchIndex + 1} of ${flatMatches.length}`}
         </span>
         <button
           type="button"
           className={`${styles.navButton} ${styles.navButtonIcon}`}
           onClick={goToPreviousMatch}
           disabled={flatMatches.length === 0}
           aria-label="Previous match"
         >
           <CaretUp size={12} aria-hidden />
         </button>
         <button
           type="button"
           className={`${styles.navButton} ${styles.navButtonIcon}`}
           onClick={goToNextMatch}
           disabled={flatMatches.length === 0}
           aria-label="Next match"
         >
           <CaretDown size={12} aria-hidden />
         </button>
         <button
           type="button"
           className={`${styles.navButton} ${styles.navButtonIcon}`}
           onClick={() => setSearchOpen(false)}
           aria-label="Close search"
         >
           <X size={12} aria-hidden />
         </button>
       </div>
     )}
     ```
     Quando a busca está fechada, mostra só o botão de alternância (ícone + "Search"), reaproveitando `.highlightsToggle` (mesmo padrão visual do botão "Chat" já existente). Quando aberta, o botão de alternância é substituído pela barra de busca — não renderizar os dois ao mesmo tempo.
     Fechar a busca (X, Escape, ou clicar em "Search" de novo — aqui só existe o botão de abrir/fechar, não precisa de toggle duplo) NÃO limpa `searchQuery` nem cancela a indexação em andamento (ela roda até o fim em background independente do painel estar visível — ver Etapa 5). Reabrir a busca mantém o termo anterior.
  2. CSS em `Reader.module.css`: inserir depois de `.zoomLabel` (linha 95) e antes de `.pageArea` (linha 97):
     ```css
     /* ─── Barra de busca no ebook ────────────────────────────────────────── */
     .searchBar {
       display: flex;
       align-items: center;
       gap: 6px;
     }

     .searchInput {
       width: 180px;
       background: var(--surface-1, #1d1d34);
       border: 1px solid var(--border, #333340);
       border-radius: 6px;
       color: var(--text-primary, #ddddf1);
       padding: 5px 8px;
       font-size: 12px;
       font-family: inherit;
     }

     .searchInput:focus {
       outline: none;
       border-color: var(--accent, #7b6ed4);
     }

     .searchCount {
       font-size: 11px;
       color: var(--text-muted, #7c7c93);
       white-space: nowrap;
       min-width: 64px;
     }

     /* Modificador de .navButton para conter um ícone (svg) em vez de um
        glifo de texto — centraliza via flex. Não altera .navButton em si
        (usado em vários lugares só com texto) para não arriscar regressão
        visual nos botões existentes. */
     .navButtonIcon {
       display: inline-flex;
       align-items: center;
       justify-content: center;
     }
     ```
- Arquivos: `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- Depende de: Etapa 4, Etapa 7

---

### Etapa 9 — Camada visual de destaque das ocorrências (Requisito 1, parte 6/6)
- Status: FEITO
- Descrição:
  1. No JSX de `.pageWrap` (linhas 1553-1583), adicionar uma nova camada logo depois do fechamento de `.highlightLayer` (`</div>`, linha 1582) e antes de `<div ref={textLayerRef} className={styles.textLayer} />` (linha 1583):
     ```tsx
     <div className={styles.searchMatchLayer} aria-hidden="true">
       {searchOpen &&
         normalizedQuery &&
         pageMatchRects.map((rects, occIdx) => {
           const isActive =
             currentMatch?.page === pageNum && currentMatch.occurrenceIndexOnPage === occIdx
           return rects.map((r, i) => (
             <div
               key={`match-${occIdx}-${i}`}
               data-match-active={isActive ? 'true' : undefined}
               className={`${styles.searchMatchRect} ${isActive ? styles.searchMatchRectActive : ''}`}
               style={{
                 left: `${r.x * 100}%`,
                 top: `${r.y * 100}%`,
                 width: `${r.w * 100}%`,
                 height: `${r.h * 100}%`,
               }}
             />
           ))
         })}
     </div>
     ```
     `data-match-active="true"` no retângulo ativo é o atributo consumido pelo effect de scroll da Etapa 7 (`querySelector('[data-match-active="true"]')`).
  2. CSS em `Reader.module.css`: inserir depois de `.highlightRect` (linha 264) e antes do comentário "Toolbar flutuante de grifo" (linha 266):
     ```css
     /* ─── Camada de destaque das ocorrências de busca ────────────────────────
        Mesma técnica do .highlightLayer (retângulos normalizados sobre a
        página atual), mas só populada quando a busca está aberta — ver
        computeSearchMatchRects. pointer-events: none para não interferir
        com seleção de texto/clique em highlights. */
     .searchMatchLayer {
       position: absolute;
       inset: 0;
       pointer-events: none;
       z-index: 2;
     }

     .searchMatchRect {
       position: absolute;
       pointer-events: none;
       border-radius: 2px;
       background: rgba(255, 165, 0, 0.35);
     }

     .searchMatchRectActive {
       background: rgba(255, 140, 0, 0.6);
       outline: 2px solid rgba(255, 111, 0, 0.9);
     }
     ```
- Arquivos: `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- Depende de: Etapa 6, Etapa 7
