# Task: Web Search via Tool Use

## Objective
Substituir a arquitetura atual de roteamento de web search — que usa um modelo intermediário (GPT-4.1-mini) para decidir se a busca é necessária — por tool use nativo do modelo respondedor, quando o modelo selecionado suportar essa capacidade. O roteador atual apresenta falhas estruturais: não consegue inferir com confiança o que o modelo respondedor sabe pelo treinamento, perde contexto em perguntas de acompanhamento (ex.: pronomes vagos como "ele" desconectados do tópico estabelecido), e adiciona latência desnecessária mesmo quando a busca não é necessária. A solução é deixar o próprio modelo respondedor decidir quando chamar a tool `web_search`, pois ele conhece seus próprios limites de conhecimento. Para modelos sem suporte a tool use, o fluxo atual (`web_search_route`) permanece como fallback.

## Context

- **Arquitetura atual:** `useChat.ts` → `web_search_route` (Rust, GPT-4.1-mini via OpenRouter) → `webSearch()` / `runDeepResearch()`
- **Arquitetura nova (modelos com tools):** `useChat.ts` envia tool definition → modelo emite `tool_call` na stream → `webSearch()` / `runDeepResearch()` → resultado injetado → stream continua
- **Arquitetura nova (modelos sem tools):** fluxo atual preservado sem alteração
- **Arquivos que mudam:** `useChat.ts`, `lib.rs` (struct `ModelInfo` e `openrouter_list_models`)
- **Arquivos que não mudam:** `search.ts`, `deepResearch.ts`
- **Toggle Deep Research:** continua funcionando via tool use — o modelo chama `deep_research` em vez de `web_search`; o pipeline interno de sub-queries e rerank não muda

## Requirements

- Capturar o campo `supports_tools` da API do OpenRouter ao listar modelos (`openrouter_list_models` em `lib.rs`), expondo-o no `ModelInfo` e no `AiModel` do frontend
- Quando o modelo selecionado suportar tool use e o toggle de web search ou deep research estiver ativo, incluir as tool definitions no payload da requisição ao OpenRouter
- Detectar `tool_calls` na stream SSE do OpenRouter e interromper a stream para executar a tool
- Para o toggle **web search**: tool `web_search(query: string)` → chama `webSearch(query, 3)` → injeta resultado → continua stream
- Para o toggle **deep research**: tool `deep_research(query: string)` → chama `runDeepResearch(query, ...)` → injeta resultado → continua stream
- Quando o modelo não suportar tool use, manter o fluxo atual via `web_search_route` sem nenhuma alteração
- Remover a chamada a `web_search_route` para modelos que suportam tool use — sem chamadas duplas
- A UI de fases do deep research (`onPhase`) deve continuar funcionando normalmente
- **UI — indicador de web search:** quando o modelo emitir um `tool_call` de `web_search`, exibir um box acima do input do usuário com o texto "Searching the web..." e um dot pulsante verde, seguindo o mesmo padrão visual das classes `deepResearchProgress` e `deepResearchDot` já existentes em `ChatPanel.module.css`. O box deve sumir automaticamente quando a busca terminar e a stream continuar, sem ação do usuário. O indicador de web search e o de deep research são mutuamente exclusivos (os toggles já garantem isso).

## Constraints

- Não alterar `search.ts` nem `deepResearch.ts`
- Não remover nem modificar o comando Rust `web_search_route` — ele é o fallback
- O tool use só deve ser ativado se o toggle correspondente (web search ou deep research) estiver ativo — não habilitar tools incondicionalmente
- Não introduzir dependências externas novas
- O estado de loading/thinking da UI deve refletir corretamente o período de execução da tool (busca em andamento)
- Manter compatibilidade com o campo `supports_vision` já existente no `ModelInfo`

## Edge Cases

- Modelo suporta tool use mas a Tavily key não está configurada: não incluir a tool definition no payload (comportamento atual: já valida a key antes de buscar)
- Modelo emite `tool_call` mas o argumento `query` vem malformado ou vazio: usar a última mensagem do usuário como fallback de query, ou ignorar silenciosamente e continuar sem busca
- Modelo emite múltiplos `tool_calls` no mesmo turno: executar apenas o primeiro (web search e deep research são mutuamente exclusivos pelos toggles)
- Deep research e web search ambos ativos: deep research tem prioridade (comportamento atual preservado)
- Stream interrompida pelo usuário (botão Stop) durante execução da tool: cancelar normalmente, sem estado corrompido
- OpenRouter não retorna `supported_parameters` para um modelo específico: tratar como `supports_tools: false` e usar o fluxo de fallback
- Modelo suporta tool use mas responde sem emitir tool_call (decidiu não buscar): continuar stream normalmente — esse é o comportamento esperado e correto
