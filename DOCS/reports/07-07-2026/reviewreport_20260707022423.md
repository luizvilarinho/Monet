# Review Report — Fase 3 do Leitor de Livros (Chat de IA com contexto de leitura)

- Data: 07-07-2026
- Branch: `feature/ebook-reader`
- Task report: `DOCS/reports/06-07-2026/taskreport_20260706100209.md`
- Execution report: `DOCS/reports/06-07-2026/executionreport_20260706100438.md`
- Plan report: `DOCS/reports/06-07-2026/planreport_20260706101636.md`

## Escopo revisado

União dos arquivos alterados/criados nas 3 sessões de `codeAgent` registradas no execution report (implementação inicial → ajuste pasta-por-livro/resize → ajustes de UI via conversa direta):

- `src/storage/index.ts`, `src/storage/tauri.ts`, `src/storage/browser.ts`
- `src/hooks/useChat.ts`
- `src/components/AssistantPanel/AssistantPanel.tsx`
- `src/components/ChatPanel/ChatSidebar.tsx` (sem diff — revertido ao original, confirmado via `git diff --stat`)
- `src/components/Library/Library.tsx`, `src/components/Library/Library.module.css`
- `src/components/Reader/Reader.tsx`, `src/components/Reader/Reader.module.css`
- `src/components/Reader/readerContext.ts` (novo)
- `src/components/Reader/ReaderChatPanel.tsx` (novo)
- `src/components/Reader/ReaderChatPanel.module.css` (novo)

Nota importante sobre escopo: o `useChat.ts` da árvore de trabalho contém uma reestruturação relevante — `ChatConversation.folderId` como fonte de verdade da vinculação conversa↔pasta, cache anti-eco `lastKnownValues`, e reescrita de `reconcileOrders` — que **não está descrita em nenhuma das 3 sessões de `codeAgent` do execution report** (a sessão 2 lista `useChat.ts` como alterado, mas seu resumo só cobre o vínculo `bookId→folderId` e o resize; não menciona a reestruturação de `folderId`). A única fonte que documenta essa mudança é a memória do projeto `project-chat-folderid-architecture.md` ("fix do bug '2º chat do Reader órfão'", 2026-07-07). O arquivo está corretamente no escopo (listado como alterado), então a revisão prosseguiu normalmente, mas isso é reportado como problema de processo abaixo.

`npx tsc --noEmit -p .` rodou limpo no estado atual da árvore de trabalho.

## Avaliação por arquivo

### `src/storage/index.ts`, `src/storage/tauri.ts`, `src/storage/browser.ts`
1. **Correção**: `getQuotes` espelha `getHighlights`/`rowToHighlight` exatamente; SQL parametrizado, `ORDER BY created_at DESC` correto, sem `scheduleCheckpoint` (leitura pura, como planejado). Schema de `book_quotes` (migration v13 em `src-tauri/src/lib.rs:1297`) bate com `BookQuoteRow`/`rowToQuote`.
2. **Simplicidade**: OK, sem alternativa mais simples.
3. **Efeitos colaterais**: nenhum — método novo, não toca comportamento existente.
4. **Padrões**: segue o padrão exato de `BookHighlightRow`/`rowToHighlight` já em uso.
5. **Segurança**: consulta parametrizada, sem risco de injection.
6. **Organização**: bloco colocado ao lado do par `saveQuote`/`deleteQuotesByBook`, coerente com a estrutura do arquivo.

Diff trivial e correto — nada a apontar além do que já está acima.

### `src/hooks/useChat.ts`
1. **Correção**: verifiquei manualmente (sem confiar só no `tsc`) os pontos de maior risco:
   - `reconcileOrders`: idempotência confirmada por inspeção (aplicar sobre o próprio output não altera nada); conversas deletadas continuam sendo removidas de `folder.conversationIds`/`looseOrder` (o `byId.get(id)` retorna `undefined` para ids inexistentes, o que já as exclui); `folderId` "pendurado" (pasta ausente) não é zerado — a conversa aparece na lista solta até a pasta reaparecer ou for confirmada ausente, evitando a órfã permanente que o fix documenta.
   - Modo `reader` (`isDetached`, fallback para `null` em vez de `conversations[0]`) preservado nos 3 pontos relevantes: estado inicial de `activeId`, effect de fallback (linha ~1024) e handler de `storage`.
   - `send()`: o bloco efêmero (`ephemeralSystemMessage`) só entra nos dois payloads da API (`apiMessages`/`toolApiMessages`) e nunca é gravado em `ChatMessage.content`/histórico — confirmado por leitura completa do corpo de `send()`.
   - `ensureReaderBookFolder`: as guardas de criação pendente (`pendingBookFolderIds`, `readerFolderIdRef`) evitam pasta duplicada em chamadas concorrentes (StrictMode, múltiplos efeitos no mesmo tick) — tracei o fluxo e não encontrei caminho que duplique.
   - Assistant/main permanecem retrocompatíveis: `useChat(models)` e `send(text[, img[, docs]])` inalterados; `startAssistantConversation()` sem argumento continua produzindo `folderId: null`, resolvido por nome no `send()` como antes.
2. **Simplicidade**: a reescrita de `reconcileOrders` é substancialmente mais complexa que a versão anterior, mas a complexidade é inerente ao problema que resolve (condição de corrida cross-window com múltiplas fontes de verdade). Não vejo uma forma mais simples de obter a mesma garantia.
3. **Efeitos colaterais**: nenhuma regressão encontrada em chat principal/assistant. `[nit]` — ver abaixo sobre `deleteConversation`.
4. **Padrões**: segue os padrões já usados no arquivo (Record em localStorage estilo `RESPONSE_LINK_KEY`, `useRef` para valores sem re-render, `useCallback` com deps corretas).
5. **Segurança**: sem exposição de dado sensível; contexto efêmero é só texto de leitura do próprio usuário.
6. **Organização**: arquivo já era grande (~1900 linhas segundo o explorer) e cresceu mais; aceitável dado o padrão já estabelecido do projeto de manter o motor de chat num único hook.

`[importante]` **Processo/documentação** — a reestruturação de `ChatConversation.folderId` como fonte de verdade, o cache anti-eco `lastKnownValues` e a reescrita de `reconcileOrders` (fix do bug "2º chat do Reader órfão") não aparecem em nenhuma sessão do execution report, apesar de estarem no arquivo em escopo. Só são rastreáveis via uma memória de projeto separada. Isso não bloqueia o merge (a mudança está correta e foi validada pelo Coordenador em teste manual), mas deixa o execution report como registro incompleto do que mudou — um mantenedor futuro lendo só os reports não saberia dessa reestruturação. Recomendo complementar o execution report com uma entrada descrevendo essa mudança antes de fechar a fase.

`[nit]` `deleteConversation` (linha ~1282) faz `setActiveIdState` cair para `remaining[0]?.id ?? null` sem checar `isDetached`, diferente do padrão "detached nunca pula para conversa alheia" aplicado em outros pontos do arquivo (linha ~1024, `deleteChatFolderById`, etc.). Hoje é inofensivo porque nem `ReaderChatPanel` nem `AssistantPanel` expõem exclusão de conversa individual (`deleteConversation` não está entre os campos desestruturados). Mas é uma armadilha latente: se um botão de "apagar esta conversa" for adicionado ao painel do leitor/assistant no futuro, reintroduziria exatamente a classe de bug que o resto deste diff corrigiu.

### `src/components/AssistantPanel/AssistantPanel.tsx`
Diff trivial (`{ isAssistant: true }` → `{ mode: 'assistant' }`), sem efeito colateral — confirmado via os demais call sites (`startAssistantConversation()`, `ensureAssistantFolder()`) que continuam funcionando com a nova assinatura.

### `src/components/ChatPanel/ChatSidebar.tsx`
Sem diff — revertido ao estado original, conforme reportado. Proteção de rename/delete continua restrita à pasta `assistant`; pastas do leitor (`Library - <título>`) ficam sujeitas a rename/delete pelo usuário como qualquer outra pasta — decisão explícita e testada (item "e"/"d" do roteiro de teste do Coordenador).

### `src/components/Library/Library.tsx`, `Library.module.css`
1. **Correção**: cascata de deleção (`getReaderChatFolderId` → `deleteChatFolderById` → `unlinkBookFromReaderFolder`) correta e sem quebrar o fluxo se algum passo falhar (as funções chamadas engolem exceções internamente e apenas logam). Fix de `table-layout: fixed` é a causa raiz correta do scroll horizontal indevido (larguras percentuais das colunas só são respeitadas com layout fixo).
2. **Simplicidade**: OK.
3. **Efeitos colaterais**: nenhum além do pretendido.
4. **Padrões**: import dos helpers de `../../hooks/useChat` segue o padrão do resto do arquivo.
5. **Segurança**: N/A.
6. **Organização**: dead code (`onNavigateToNote`) removido da interface `ReaderProps`, da desestruturação em `Reader.tsx` e do call site em `Library.tsx` — confirmado via grep, zero referências residuais.

### `src/components/Reader/Reader.tsx`, `Reader.module.css`
1. **Correção**: revisão detalhada de:
   - `getPageText` com cache por página, resetado ao trocar de documento (`useEffect` em `[doc]`) — não trava a navegação porque só roda sob demanda no envio do chat.
   - `jumpToPage`/`jumpBackPage`: marca o retorno só em navegação "de citação" (grifo/sumário), nunca em `‹›`/input de página; limpa sozinho ao voltar à página de origem por qualquer via — comportamento correto e testado.
   - Captura de cópia dupla (botão Copy + evento `copy` nativo): confirmei que `navigator.clipboard.writeText` não dispara `copy`, então não há registro duplicado; o handler de `copy` valida seleção não-colapsada e contida em `textLayerRef` antes de gravar, e nunca lê o clipboard do SO (cumprido o requisito de privacidade do task report).
   - Resolução do sumário nativo do PDF (`getOutline`/`getDestination`/`getPageIndex`) com tratamento de erro (`try/catch` retornando `null`) e tipagem derivada via `Parameters<...>` para contornar a ausência de `RefProxy` no entrypoint público do pdfjs-dist — solução razoável, compila limpo.
   - Coluna de navegação (`sidePanel`) sempre montada e retrátil, coluna do chat sempre montada e escondida via CSS — ambas preservam estado/stream ao alternar visibilidade, conforme exigido pelo task report.
2. **Simplicidade**: `OutlineList` extraído como componente recursivo próprio — boa decomposição, não haveria forma mais simples dado que o outline é uma árvore.
3. **Efeitos colaterais**: nenhuma regressão nos fluxos de grifo/citação existentes da Fase 2 (grep confirma zero referências residuais a `highlightsPanelOpen`/`highlightsToggleBadge`).
4. **Padrões**: CSS module segue a convenção do arquivo; ícones via `@phosphor-icons/react` como já usado no restante do projeto.
5. **Segurança**: N/A.
6. **Organização**: boa separação entre lógica pura de outline (funções de topo de arquivo) e o componente.

`[nit]` A pasta de chat do livro é criada de forma **eager** — o efeito de mount do `ReaderChatPanel` chama `ensureReaderBookFolder` incondicionalmente, mesmo com o toggle "Chat" fechado (o painel fica sempre montado, só escondido via CSS). Ou seja, simplesmente abrir um livro para leitura já cria/persiste uma pasta "Library - <título>" no Chat principal, mesmo que o usuário nunca use o chat daquele livro. Isso é exatamente o comportamento testado e aceito pelo Coordenador (item "(a) abrir livro cria/retoma pasta com o título do livro no Chat principal" no roteiro de teste), então não é reportado como bug — só registro do trade-off: usuários que leem muitos livros sem usar IA acumulam pastas vazias na Sidebar do Chat principal.

`[nit]` Discrepância cosmética entre o relatado e o implementado: o execution report descreve o nome da pasta do leitor como "nome = título do livro"; o código de fato usa `Library - ${title}` (prefixo fixo). Não é um bug, só uma imprecisão na documentação do execution report.

### `src/components/Reader/readerContext.ts`
1. **Correção**: implementa fielmente as regras do plano — orçamentos (`PAGE_TEXT_BUDGET=6000`, `MAX_HIGHLIGHTS=20`, `MAX_QUOTES=20`, `ITEM_TEXT_BUDGET=300`), truncamento com marcador `[truncated]`, mensagem fixa para PDF escaneado, omissão de seções vazias, nunca lança exceção. Função pura, sem I/O — fácil de testar isoladamente (embora não haja testes automatizados no projeto, consistente com o padrão atual do repositório).
2. **Simplicidade**: direta, sem gordura.
3. **Efeitos colaterais**: nenhum (função pura nova).
4. **Padrões**: nomeação e organização (constantes no topo, helpers privados) consistentes com o resto do projeto.
5. **Segurança**: N/A — o texto vem só de dados do próprio usuário (livro/grifos/cópias locais).
6. **Organização**: módulo dedicado corretamente separado da UI (`ReaderChatPanel`), boa decisão de arquitetura.

### `src/components/Reader/ReaderChatPanel.tsx`, `ReaderChatPanel.module.css`
1. **Correção**: reuso fiel do padrão `AssistantPanel` (carregamento de modelos, banner de API key, bolha de mensagem com markdown sanitizado via `renderMarkdown` existente, tratamento de link só para `http(s)` via `openUrl`). Resize por arrasto espelha corretamente o padrão do `ChatSidebar` (delta invertido por o painel estar à direita). Seletor de conversas da pasta (`folderConversations`) filtra corretamente só conversas do livro atual — não há vazamento de contexto entre livros. Recuperação de pasta apagada em outra janela e recuperação mid-session (`activeId` caindo para `null`) tratadas com efeitos dedicados.
2. **Simplicidade**: replicar a bolha de chat localmente (em vez de extrair um componente compartilhado com `AssistantPanel`) é aceitável — o próprio plano já autorizava essa escolha por ser pequeno.
3. **Efeitos colaterais**: nenhum fora do escopo do painel.
4. **Padrões**: reuso de `FolderSystemPromptModal`/`FolderDocumentSelectorModal`/`FolderMemoryModal`/`ChatToolsMenu`/`ModelSelector` exatamente como no `AssistantPanel`.
5. **Segurança**: mesma sanitização de markdown já usada no resto do chat (rehype-sanitize), nenhum novo vetor de XSS introduzido.
6. **Organização**: arquivo coeso, único componente + bolha auxiliar no mesmo arquivo (padrão do `AssistantPanel`).

## Bugs extras
Nenhum encontrado fora do escopo, dentro do código que precisei ler para a revisão (App.tsx, ChatSidebar.tsx, pdf.ts, markdown.ts).

## Veredicto final

**Veredicto: APROVADO COM RESSALVAS**

A implementação está correta nos pontos de maior risco (isolamento do contexto efêmero, retrocompatibilidade do `useChat`, idempotência do `reconcileOrders`, ausência de vazamento de contexto entre livros/conversas) e passa em `tsc --noEmit` limpo. As ressalvas são: (1) a reestruturação de `folderId`/anti-eco em `useChat.ts` não está documentada em nenhuma sessão do execution report — recomendo complementar o registro antes de fechar a fase; (2) um ponto de fragilidade latente em `deleteConversation` (fallback de `activeId` não seguro para instâncias detached, hoje inofensivo por falta de UI que o exercite); (3) dois nits cosméticos/de trade-off já cobertos pelo teste do Coordenador. Nada bloqueia o merge.
