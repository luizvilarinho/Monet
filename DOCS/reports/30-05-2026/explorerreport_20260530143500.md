# Explorer Report — Notes Area 9 Adjustments

**Timestamp:** 20260530143500  
**Scope:** Mapping code locations for 9 items across Notes area (drag handles, search, Calendar, breadcrumb, spell-check, calendar dots, slash commands)

---

## Item 1 — Drag handle for TODO items

**Status:** Feasible | **Difficulty:** Low-Medium

### Current State
- Drag handle mechanism exists for /command blocks (slash command responses)
- Located in: src/components/Editor/CommandExtension.ts
  - Function makeDragHandle() (lines 318–428) creates the drag handle widget
  - Uses Decoration.widget() from Tiptap to render the handle with mousedown/mousemove/mouseup handlers
  - Handle rendered at pos + 1 (before paragraph) with class monetCmdDragHandle (line 324)
  - Full drag-and-drop logic: position computation, drop target detection, slice-move operations
- CSS styling in: src/components/Editor/Editor.module.css (lines 365–391)

### TODO/TaskList Implementation
- Uses Tiptap built-in extensions:
  - TaskList from @tiptap/extension-task-list (imported in src/components/Editor/Editor.tsx line 4)
  - TaskItem from @tiptap/extension-task-item (line 3)
  - TaskItem configured with 
ested: false (line 182)
- CSS for task list in: src/components/Editor/Editor.module.css (lines 274–330)
  - Renders as ul[data-type="taskList"] with li > label > input[type="checkbox"]
  - Checkbox styling handled with :checked pseudo-class and custom after element

### Why TODO Lacks Drag Handle
- TaskList items rendered by Tiptap's native extension (no custom node type)
- No custom decoration plugin targeting TaskItem nodes
- CommandExtension only decorates paragraphs matching command pattern (line 512–567)

### Implementation Path
**Approach:** Extend CommandExtension to also decorate TaskItem nodes with the same drag handle widget
- Modify uildDecorations() to detect and iterate over 	askItem nodes (in addition to paragraphs)
- Reuse makeDragHandle() function for TaskItem nodes
- **Risk:** TaskItem nodes may have different structure than paragraphs (nested list items); may need adapted position logic
- **Alternative:** Create a separate extension specifically for TaskItem drag handles to keep concerns separated

---

## Item 2 — Advanced search (Ctrl+P): prioritized match display

**Status:** Feasible | **Difficulty:** Low

### Current State
- Component: src/components/SearchPalette/SearchPalette.tsx
  - Uses storage.searchNotes(query) (line 43)
  - Results displayed in order returned from backend (line 92–109)
  - Only shows note title and content preview (stripMarkdown helper, lines 12–19)
- Backend search in: src/storage/tauri.ts (lines 276–288)
  - Simple SQL LIKE query on 	itle, content, 	ags (case-insensitive)
  - Results ordered by updated_at DESC
  - Returns Note[] (no notebook/subject info attached)

### Missing Information
- Note struct (line 106–129 in src/storage/tauri.ts) includes:
  - 
otebook_id: string | null
  - subject_id: string | null
  - But these are not populated in the owToNote() conversion (lines 119–129)
- Notebooks and subjects must be loaded separately and matched in frontend

### Implementation Path
**Option A (Frontend sorting):**
- Fetch all notebooks and subjects in SearchPalette component
- After storage.searchNotes() returns results, sort results in-frontend:
  1. Notebook name match (exact or contains query)
  2. Note title match (exact or contains query)
  3. Body/tag matches
- Render notebook name above note title using a lookup map

**Option B (Backend sorting):**
- Modify searchNotes() in src/storage/tauri.ts to JOIN notebooks and subjects
- Return extended result with notebook name included
- Let frontend display notebook name as muted label above note title

**Recommendation:** Option A (lighter backend change, frontend logic is simple)

---

## Item 3 — All Notes: hide Calendar notes

**Status:** Feasible | **Difficulty:** Low

### Current State
- "All Notes" list in: src/components/NotebookList/NotebookList.tsx (line 437)
  - Rendered as first item: <li className={...} onClick={() => onSelect(null)}>
  - Clicking "all notes" sets ctiveId = null
- Calendar notebook ID stored in localStorage and passed down as calendarNotebookId prop (line 255)
- In src/App.tsx:
  - Calendar notebook filtered out from main notebook list (line 462: 
otebooks.filter((n) => n.id !== calendarNotebookId))
  - But "All Notes" shows ALL notes (including Calendar) because filter only applies to the notebook list UI, not to notes data

### Note Filtering Logic
- src/App.tsx (lines 427–452):
  - subjectFilteredNotes: filters by activeNotebookId and activeSubjectId
  - When ctiveNotebookId === null, returns all notes (line 430: eturn notes)
  - No explicit filter for Calendar notes when in "All Notes" view

### Implementation Path
**Option A (Collapsible Calendar section):**
- In "All Notes" view, show two sections:
  1. Regular notes (filtered 
otes.filter(n => n.notebookId !== calendarNotebookId))
  2. Calendar section (collapsible) with filtered notes
- Requires UI component changes in NotebookList or a new Sidebar component structure
- State management for collapse toggle (localStorage or React state)

**Option B (Remove entirely):**
- Filter Calendar notes from "All Notes": 
otes.filter(n => n.notebookId !== calendarNotebookId) in App.tsx line 430
- Simpler, no UI changes needed
- May be preferred if Calendar notes are meant to be isolated

**Recommendation:** Option B unless UX specifically requires discoverability (Option A would require more refactoring)

---

## Item 4 — BUG: Subject name missing in note header (All Notes context)

**Status:** Identified | **Difficulty:** Low

### Root Cause
- In src/App.tsx (line 980):
  `
  subjectName={subjects.find(s => s.id === activeNote.subjectId)?.name}
  `
- This line is executed for ALL note opens, but the condition is correct
- The bug likely occurs when ctiveNote.subjectId is 
ull or when activeNote is opened from "All Notes" vs. notebook context

### Current Breadcrumb Rendering
- In src/components/Editor/Editor.tsx (lines 624–626):
  `
  {notebookName && (
    <span className={styles.notebookLabel}>
      {notebookName}{subjectName ? ' | {subjectName}' : ''}
  `
- The breadcrumb only renders if 
otebookName is truthy
- When a note is opened from "All Notes" (activeId from search), ctiveNotebookId remains set to the previous notebook or 
ull

### Why Subject Disappears in "All Notes" Flow
- When clicking a note from search results (SearchPalette), onSelectNote(notebookId, noteId) is called
- This should navigate to the notebook AND select the note
- But if the note belongs to a different notebook than the currently active one, the subject lookup fails

### Implementation Path
- Verify that ctiveNotebookId is updated when selecting a note from "All Notes"
- In src/App.tsx, the handler for search note selection likely needs to:
  1. Set ctiveNotebookId = note.notebookId (not just ctiveId = note.id)
  2. Set ctiveSubjectId = note.subjectId ?? null
- Check the onSelectNote callback passed to SearchPalette

---

## Item 5 — BUG: Spell-check context menu broken

**Status:** Identified | **Difficulty:** Medium

### Current State
- Context menu rendered in: src/components/Editor/Editor.tsx (lines 731–774)
  - Triggered by contextmenu event (line 528)
  - Currently only shows "Link note" button (line 771)
- Spell-check enforcer in: src/lib/spellcheck.ts:
  - Simply toggles spellcheck attribute to force OS spell-check reactivation
  - Does NOT integrate with context menu

### Expected Browser Behavior
- Native Windows spell-check shows suggestions in context menu
- When contentEditable element has spellcheck="true", right-click on red-underlined word shows suggestions
- **Problem:** Custom context menu (line 731–774) intercepts the event with e.preventDefault() (line 525)
- This prevents browser from showing native spell-check suggestions

### Implementation Path
**Challenge:** Integrating browser spell-check suggestions with custom UI
1. **Option A (Disable custom menu for spell-checked words):**
   - Detect if right-click target is a spell-checked word (has red underline)
   - If true, don't prevent default; let browser show suggestions + custom menu
   - May require checking DOM node for spell-check styling

2. **Option B (Use a spell-check library):**
   - Integrate 	ypo.js or similar client-side spell-check
   - Parse suggestions programmatically and add to custom menu
   - More control, but adds dependency

3. **Option C (Remove custom menu):**
   - Let browser handle spell-check natively
   - Only show "Link note" on other right-clicks (non-spell-check targets)
   - Requires detecting spell-check underlines

**Recommendation:** Option A (minimal change, preserves existing UX)

---

## Item 6 & 7 — Calendar dots (red and purple)

**Status:** Feasible | **Difficulty:** Medium

### Current Dot Logic
- In src/components/CalendarView/CalendarView.tsx:
  - Line 90–98: 
otedDays set = notes with title matching DD/MM/YYYY format
  - Line 100–106: datedDaysSet = notes with 
ote.date field set (format YYYY-MM-DD)
  - Line 235: hasNote = cell.current && (notedDays.has(title) || datedDaysSet.has(dateKey))
  - Line 261: {hasNote && <span className={styles.dot} aria-hidden />}
- CSS: src/components/CalendarView/CalendarView.module.css (lines 204–211)
  - Single dot color: ackground: #e05555 (red)

### Requirement Analysis
**Item 6:** Red dot only when note has content
- Current logic: dot appears if 
ote.date field is set (regardless of content)
- **Fix:** Check if note content is non-empty before marking hasNote
- Challenge: "empty" in Tiptap = single <p></p> (default paragraph), not just ""

**Item 7:** Purple dot for notes with unchecked TODOs
- Requires scanning note content for TaskList items
- Need to count checked vs. unchecked todos
- Render purple dot instead of red if any unchecked TODOs exist

### Implementation Path
1. **Content check (Item 6):**
   - Create helper function: hasContentInNote(note: Note): boolean
   - Check if 
ote.content.trim() is not empty
   - Or parse the Tiptap JSON to check for non-empty text nodes (more robust for Tiptap's internal format)

2. **TODO check (Item 7):**
   - Create helper: hasUnfinishedTodos(note: Note): boolean
   - Parse note.content (Markdown or JSON) to find TaskList items
   - Count unchecked items ([ ] pattern in Markdown or TaskItem with checked: false in JSON)
   - Modify dot color based on: unchecked > 0 ? purple : red

3. **Rendering:**
   - Modify line 261 to render two types of dots or conditional color
   - Add CSS class .dotPurple with ackground: #9370db (or theme purple)

**Risk:** Content parsing in frontend (Markdown vs. internal JSON format); may need clarification on how notes are stored internally

---

## Item 8 — Slash command: /action

**Status:** Feasible | **Difficulty:** Low

### Current Slash Command Structure
- Definitions: src/lib/commands.ts (lines 3–16)
  - Each command is a CommandDef object with properties:
    - 
ame: command string (e.g., /search)
    - description: UI label
    - example: example usage
    - 	akesQuery: boolean (true = command takes additional text)
    - usesSearch?: boolean (optional, for commands using web search)
- 12 commands defined; /mindmap at line 13 (no query)

### Execution Flow
- Commands parsed in: src/components/Editor/commandParser.ts
- Executed in: src/App.tsx handleCommand() (lines 478–620)
  - Looks up command definition with indCommand(cmd)
  - Calls OpenRouter API via useAi hook
  - Response embedded in editor as embedBlock

### Adding /action Command
1. **Add to COMMANDS array** (src/lib/commands.ts):
   Add new entry with name /action, description, example, takesQuery: true
2. **No code changes needed** in execution flow—handleCommand() handles all commands generically via OpenRouter
3. **System prompt** might need tuning to understand "action" semantics
   - Current system prompt likely in src/hooks/useAi.ts or src/lib/anthropic.ts
   - May need to specify what "action" means (edit in-place, transform, etc.)

---

## Item 9 — Slash command: verify /mindmap naming

**Status:** Uncertain | **Difficulty:** Low

### Current State
- /mindmap defined at line 13 of src/lib/commands.ts:
  `
  { name: '/mindmap', description: 'Generates a hierarchical mindmap of the current note in markdown', example: '/mindmap', takesQuery: false }
  `
- **Description says "hierarchical mindmap"** but also mentions "markdown"
- No slash command for /roadmap exists

### Investigation Needed
- Query the system prompt or OpenRouter API response to understand what format is actually generated
- Possible outcomes:
  1. /mindmap generates a visual mindmap structure (hierarchical bullets) – keep name as-is
  2. /mindmap generates a roadmap with steps/sequence – rename to /roadmap
  3. Both concepts needed – keep /mindmap and add separate /roadmap command

### No Code Changes Until Clarified
- If rename needed: change line 13 in src/lib/commands.ts
- If new command needed: add entry and ensure system prompt differentiates between them
- **Recommend:** Run /mindmap command manually, examine output format before deciding

---

## Technical Dependencies & Risks

### Drag Handle (Item 1)
- **Risk:** TaskItem node structure differs from paragraph; may need custom positioning logic
- **Mitigation:** Test with both node types; fallback to no drag for TaskItems if complex

### Search Results (Item 2)
- **Risk:** Loading all notebooks/subjects for every search may be slow
- **Mitigation:** Cache in SearchPalette component; use React.useMemo()

### Calendar Dots (Items 6 & 7)
- **Risk:** Content parsing (detecting "empty" note or TODOs) must align with how Tiptap stores data
- **Mitigation:** Clarify internal format (Markdown text vs. Tiptap JSON); test with real notes

### Spell-check Menu (Item 5)
- **Risk:** Browser spell-check behavior varies by OS/browser; Windows may differ from Linux/Mac
- **Mitigation:** Test on target OS (Windows 11, per project scope)

### Subject in Breadcrumb (Item 4)
- **Risk:** May be a flow issue (how "All Notes" selects notes) rather than rendering issue
- **Mitigation:** Verify that onSelectNote in SearchPalette updates both ctiveNotebookId and ctiveId

---

## Summary by Difficulty

| Item | Title | Difficulty | Notes |
|------|-------|-----------|-------|
| 1 | Drag handle TODO | Low-Med | Extend existing CommandExtension; TaskItem node structure may vary |
| 2 | Search prioritization | Low | Frontend sorting + notebook name display; minimal backend changes |
| 3 | All Notes hide Calendar | Low | Filter notes in App.tsx or add collapsible UI section |
| 4 | Subject in breadcrumb | Low | Likely a flow/navigation issue; may require testing onSelectNote callback |
| 5 | Spell-check menu | Medium | Browser integration challenge; needs OS-specific testing |
| 6 | Red dot content check | Medium | Content parsing (empty note detection) aligns with Tiptap format |
| 7 | Purple dot TODOs | Medium | TODO parsing; extends Item 6 |
| 8 | /action command | Low | Add to COMMANDS array; no execution logic change needed |
| 9 | /mindmap naming | Low | Requires output inspection; likely rename or add /roadmap |

---

## Critical Questions for Clarification

1. **Item 4:** When a note is selected from "All Notes" search results, is ctiveNotebookId updated to the note's notebook?
2. **Item 6-7:** How are note contents stored/formatted internally—as Markdown text or Tiptap JSON?
3. **Item 9:** What does /mindmap actually output?
4. **Item 1:** Can TaskItem nodes be reordered like command blocks, or should they remain part of the list structure?

---

## Recommended Execution Order

1. **Items 2, 8, 3** (Low complexity, no dependencies)
2. **Item 4** (Investigate + potential quick fix)
3. **Item 9** (Inspection-based decision)
4. **Item 1** (Drag handle medium complexity)
5. **Items 5, 6, 7** (Integration work; save for last)
