# Plano

## Escopo
Implementar o slash command `/week`, disponível somente em notas do notebook Calendar (`notebookId === 'calendar'`), que reúne o conteúdo das notas de calendário (título `DD/MM/YYYY`) dos últimos 7 dias — âncora: data da nota atual, inclusive ela mesma — e usa esse conjunto como bloco de contexto adicional para o modelo responder perguntas livres sobre a semana. Reaproveita o sistema de comandos existente (`COMMANDS`, `commandParser.ts`, `CommandExtension.ts`, `handleCommand`/`buildUserMessage` em `App.tsx`) sem criar pipeline de RAG/embeddings para notas. Restrição por notebook é um mecanismo novo (não existia antes) e precisa ser propagada por toda a cadeia: definição do comando → autocomplete → validação de execução no editor → execução em `App.tsx`.

## Legenda de status das etapas
- `PENDENTE` — etapa ainda não iniciada
- `FEITO` — etapa concluída sem ressalvas
- `REVISAR` — etapa executada, mas ainda depende de ajuste

## Etapas

### Etapa 1
- Status: PENDENTE
- Descrição: Em `src/types/index.ts`, adicionar o campo opcional `calendarOnly?: boolean` à interface `CommandDef` (atualmente linhas 81-87), logo após `usesSearch?: boolean`. Esse campo marca comandos cujo uso é restrito a notas do notebook Calendar.
- Arquivos: `src/types/index.ts`
- Depende de: [-]

### Etapa 2
- Status: PENDENTE
- Descrição: Em `src/lib/commands.ts`, adicionar uma nova entrada ao array `COMMANDS` para o comando `/week`, no mesmo padrão das demais entradas:
  `{ name: '/week', description: 'Answers free-form questions using the last 7 days of calendar notes', example: '/week what were my activities this week?', takesQuery: true, calendarOnly: true }`.
  Não incluir a propriedade `usesSearch` (omitir, como já é feito em `/define`, `/summarize`, `/opinion`, `/table`, `/mindmap`, `/docs`, `/action` — `undefined` já equivale a "não usa busca"). Não alterar nenhuma entrada existente.
- Arquivos: `src/lib/commands.ts`
- Depende de: [1]

### Etapa 3
- Status: PENDENTE
- Descrição: Em `src/components/Editor/commandParser.ts`, propagar a restrição de notebook para as funções que decidem o que é sugerido/executável:
  1. Em `getCommandLineStatus(line: string)` (linha 70-76), adicionar um segundo parâmetro `isCalendarNote = false`. Após o check `if (!parsed.definition) return 'invalid'`, adicionar: `if (parsed.definition.calendarOnly && !isCalendarNote) return 'invalid'` — isso garante que `/week` fora do notebook Calendar tenha o mesmo status (`'invalid'`) que um comando inexistente, atendendo ao edge case "mesmo comportamento de um texto não-comando".
  2. Em `getCommandSuggestions(input: string)` (linha 78-83), adicionar um segundo parâmetro `isCalendarNote = false`. Filtrar `COMMAND_NAMES` removendo nomes cujo `CommandDef.calendarOnly` seja `true` quando `isCalendarNote` for `false` (usar `findCommand` já importado para resolver cada nome ao seu `CommandDef`). O filtro deve ser aplicado tanto no caso `normalized === '/'` (retorno de todos os nomes) quanto no `filter` por prefixo.
  3. Em `getToggleTitle` (linhas 22-38), adicionar a entrada `'/week': 'Weekly answer generated by AI'` ao objeto `map`.
  Não alterar `parseCommandLine` nem `findCommand` — eles continuam fazendo apenas o lookup estrutural, sem conhecimento de notebook.
- Arquivos: `src/components/Editor/commandParser.ts`
- Depende de: [1, 2]

### Etapa 4
- Status: PENDENTE
- Descrição: Em `src/components/Editor/CommandExtension.ts`, na função exportada `getCurrentCommandLine(state: EditorState)` (linhas 834-856), adicionar um segundo parâmetro `isCalendarNote = false` e repassá-lo para a chamada interna `getCommandLineStatus(text)` (linha 852), tornando-a `getCommandLineStatus(text, isCalendarNote)`. Nenhuma outra função do arquivo (`resolveExecutedKey`, `syncExecutedMarks`, `buildCommandIndex`, etc.) precisa de alteração — elas usam `parseCommandLine` diretamente, que não muda.
- Arquivos: `src/components/Editor/CommandExtension.ts`
- Depende de: [3]

### Etapa 5
- Status: PENDENTE
- Descrição: Em `src/components/Editor/Editor.tsx`, propagar se a nota atual pertence ao notebook Calendar:
  1. Adicionar `isCalendarNote?: boolean` à interface `EditorProps` (linhas 45-63), próximo de `notebookNotes`/`onNavigateToNote`.
  2. Na desestruturação de props do componente `Editor` (linhas 119-137), adicionar `isCalendarNote = false`.
  3. Junto aos demais refs que existem para evitar closures obsoletas dentro do `useEditor` (que é criado uma única vez — ver `onChangeRef`, `onCommandRef`, `autocompleteRef`, `responsesRef`, `onRemoveResponseRef`, `onNavigateToNoteRef`, linhas 151-165), adicionar:
     ```
     const isCalendarNoteRef = useRef(isCalendarNote)
     isCalendarNoteRef.current = isCalendarNote
     ```
  4. Substituir todas as 4 chamadas a `getCurrentCommandLine(...)` no arquivo (linhas ~276, ~309, ~374, ~392) por `getCurrentCommandLine(view.state, isCalendarNoteRef.current)` ou `getCurrentCommandLine(editor.state, isCalendarNoteRef.current)`, conforme o caso (mantendo o objeto de estado já usado em cada chamada, apenas acrescentando o segundo argumento).
  5. Substituir a chamada `getCommandSuggestions(info.text)` (linha ~406) por `getCommandSuggestions(info.text, isCalendarNoteRef.current)`.
  - Observação: como o `Editor` é montado com `key={activeNote.id}` em `App.tsx` (remonta a cada troca de nota), o uso de ref aqui é por consistência com o padrão já estabelecido no arquivo, não estritamente necessário para corrigir um bug de stale closure neste caso — mas deve ser seguido para manter uniformidade com o restante do arquivo.
- Arquivos: `src/components/Editor/Editor.tsx`
- Depende de: [4]

### Etapa 6
- Status: PENDENTE
- Descrição: Em `src/App.tsx`, preparar o conteúdo (system prompt, builder de mensagem e helpers de data/semana) que `/week` vai usar:
  1. Em `buildSystemPrompt` (linhas 86-113), na seção "Command behavior:", adicionar uma nova linha ao final (logo após a linha do `/docs`, antes do fechamento da template string), com o texto:
     `- /week: respond using exclusively the "Calendar notes from the last 7 days" block as context. Answer the user's free-form question about their week (activities, plans, patterns) based only on those notes. If they do not contain enough information, say so plainly.`
  2. Atualizar a assinatura de `buildUserMessage` (linhas 155-173) para aceitar um sétimo parâmetro opcional `weekContext?: string`, incluindo seu bloco no array `parts` (mesmo padrão de `ragContext`/`searchContext`):
     ```ts
     function buildUserMessage(
       command: string,
       description: string,
       query: string,
       noteContent: string,
       searchContext?: string,
       ragContext?: string,
       weekContext?: string
     ): string {
       const parts: string[] = []
       parts.push(`Command: ${command} - ${description}`)
       if (query.trim()) parts.push(`Parameter: ${query.trim()}`)
       if (ragContext) parts.push(`\n${ragContext}`)
       if (searchContext) parts.push(`\n${searchContext}`)
       if (weekContext) parts.push(`\n${weekContext}`)
       const cleanContent = stripCommandLines(noteContent)
       if (cleanContent) {
         parts.push(`\nCurrent note content:\n${cleanContent}`)
       }
       return parts.join('\n')
     }
     ```
  3. Adicionar, próximo das demais funções utilitárias de topo de arquivo (perto de `stripCommandLines`/`expandEmbedBlocks`/`formatRagContext`, linhas 119-153), as seguintes funções novas:
     ```ts
     const CALENDAR_TITLE_RE = /^(\d{2})\/(\d{2})\/(\d{4})$/

     function formatCalendarTitle(date: Date): string {
       const d = String(date.getDate()).padStart(2, '0')
       const m = String(date.getMonth() + 1).padStart(2, '0')
       const y = date.getFullYear()
       return `${d}/${m}/${y}`
     }

     function parseCalendarTitle(title: string): Date | null {
       const match = title.trim().match(CALENDAR_TITLE_RE)
       if (!match) return null
       const day = Number(match[1])
       const month = Number(match[2])
       const year = Number(match[3])
       const date = new Date(year, month - 1, day)
       if (date.getFullYear() !== year || date.getMonth() !== month - 1 || date.getDate() !== day) {
         return null
       }
       return date
     }

     function stripEmbedBlockTags(content: string): string {
       if (!content.includes('<embed-block')) return content
       return content.replace(/<embed-block\b[^>]*><\/embed-block>/g, '')
     }

     function getWeekCalendarNotes(
       notes: Note[],
       anchor: Date
     ): { dateLabel: string; content: string }[] {
       const byTitle = new Map(
         notes
           .filter((n) => n.notebookId === CALENDAR_NOTEBOOK_ID)
           .map((n) => [n.title, n] as const)
       )
       const entries: { dateLabel: string; content: string }[] = []
       for (let i = 6; i >= 0; i--) {
         const day = new Date(anchor)
         day.setDate(day.getDate() - i)
         const title = formatCalendarTitle(day)
         const note = byTitle.get(title)
         if (!note) continue
         const content = stripCommandLines(stripEmbedBlockTags(note.content)).trim()
         if (!content) continue
         entries.push({ dateLabel: title, content })
       }
       return entries
     }

     function formatWeekContext(entries: { dateLabel: string; content: string }[]): string {
       if (entries.length === 0) return ''
       const blocks = entries.map((e) => `[${e.dateLabel}]\n${e.content}`)
       return `Calendar notes from the last 7 days:\n\n${blocks.join('\n\n')}\n\n---`
     }
     ```
     `getWeekCalendarNotes` percorre os 7 dias (do mais antigo ao mais recente, âncora inclusive) usando o título `DD/MM/YYYY` como chave; dias sem nota correspondente ou com nota de conteúdo vazio (após remover linhas de comando e tags de embed-block) são omitidos silenciosamente, atendendo aos edge cases do Task Report.
  4. Refatorar `handleCalendarDayClick` (linhas 746-772) para usar o novo helper em vez de montar a string manualmente: substituir
     ```ts
     const d = String(date.getDate()).padStart(2, '0')
     const m = String(date.getMonth() + 1).padStart(2, '0')
     const y = date.getFullYear()
     const title = `${d}/${m}/${y}`
     ```
     por
     ```ts
     const title = formatCalendarTitle(date)
     ```
     Comportamento deve permanecer idêntico (mesma formatação de string).
- Arquivos: `src/App.tsx`
- Depende de: [1, 2]

### Etapa 7
- Status: PENDENTE
- Descrição: Em `src/App.tsx`, conectar a execução de `/week` em `handleCommand` (linhas 507-651) e propagar a flag de contexto para o `Editor`:
  1. Logo após `const def = findCommand(cmd); if (!def) return false` (linhas 510-511), adicionar a guarda de notebook:
     ```ts
     if (def.calendarOnly && activeNote?.notebookId !== CALENDAR_NOTEBOOK_ID) {
       return false
     }
     ```
     Isso é defesa em profundidade: o editor já impede que `/week` seja executável fora do notebook Calendar (Etapas 3-5), mas `handleCommand` não deve confiar apenas nisso.
  2. Antes da construção final de `userMessage` (linha 634), e depois da seção que já calcula `noteContent`/`searchContext`/`isDocsOnly` (linhas 548-559), adicionar:
     ```ts
     const isWeekOnly = cmd === '/week'
     let weekContext: string | undefined
     if (isWeekOnly) {
       const anchor = activeNote ? parseCalendarTitle(activeNote.title) : null
       if (!anchor) {
         addErrorCard(
           activeId,
           cmd,
           query,
           "This note's title is not a valid calendar date (DD/MM/YYYY)."
         )
         return true
       }
       const weekEntries = getWeekCalendarNotes(notes, anchor)
       weekContext = formatWeekContext(weekEntries)
     }
     ```
  3. Substituir o bloco atual de construção de `userMessage` (linhas 634-636):
     ```ts
     const userMessage = isDocsOnly
       ? buildUserMessage(cmd, def.description, query, '', undefined, ragContext)
       : buildUserMessage(cmd, def.description, query, noteContent, searchContext, ragContext)
     ```
     por:
     ```ts
     const userMessage = isDocsOnly
       ? buildUserMessage(cmd, def.description, query, '', undefined, ragContext)
       : isWeekOnly
         ? buildUserMessage(cmd, def.description, query, '', searchContext, ragContext, weekContext)
         : buildUserMessage(cmd, def.description, query, noteContent, searchContext, ragContext)
     ```
     (Para `/week`, `noteContent` é passado vazio porque a nota do dia âncora já está incluída dentro de `weekContext` com seu próprio rótulo de data — evita duplicar o mesmo conteúdo em dois blocos.)
  4. Adicionar `notes` ao array de dependências do `useCallback` de `handleCommand` (linha 650): `[activeId, activeNote, modelId, models.length, start, addErrorCard, notes]`.
  5. No JSX onde `<Editor />` é renderizado (linhas 1011-1042), adicionar a prop `isCalendarNote={activeNote.notebookId === calendarNotebookId}` (mesma comparação já usada nas linhas 1032 e 1038 para `notebookNotes`/`relatedContent`).
- Arquivos: `src/App.tsx`
- Depende de: [5, 6]
